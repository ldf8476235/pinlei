# 开发清单-品类诊断后端改写

## 一、项目基本信息
- 项目名称：`category-analysis`（品类诊断后端改写）
- 项目路径：`H:\project_sihuo\new\backend\category-analysis`
- 开发分支：`feature/diagnosis-backend-rewrite`
- 后端技术栈：`Spring Boot + MyBatis-Plus + SQL Server + Redis`
- 唯一数据源：`SQL Server (diagnosis)`
- 配套项目：
- 前端：`H:\project_sihuo\new\ui\plus-ui`
- 旧版参考：`H:\project_sihuo\new\资料\nodejs_code`

## 二、问题记录（持续更新）
### P0（阻塞）
- [ ] 本地环境缺少 `mvn` 命令，无法执行编译与单测验证
- 影响：无法完成本地编译闭环
- 处理：安装 Maven 并加入 `PATH` 后执行：`mvn -pl category-analysis-modules/category-analysis-diagnosis/category-analysis-diagnosis-api -am -DskipTests compile`

### P1（高优先）
- [ ] `POST /api/categories/filter-options` 未最终按字典口径完成联调验收
- [ ] `POST /api/categories/tree/children` 计划取消（后续确认前端是否仍依赖）
- [ ] `POST /api/v1/diagnosis/sessions` 门店/品类筛选参数与批处理过滤链路尚未完成端到端验收
- [ ] `GET /api/v1/diagnosis/overview` 对齐 `/salesStoreClass/classPerformance` 仍有大量字段未完成赋值

### P2（中优先）
- [ ] `class_role`、`skuAbnormal` 业务口径待最终确认
- [x] `classPerformance` 字段口径（`turnoverRate`、`classSku`、`avgInventory`）已冻结

## 三、已完成事项（里程碑）
- [x] 诊断模块基础分层与接口骨架已建立
- [x] 项目收敛为单 SQL Server 数据源（`diagnosis`）
- [x] 品类树接口：`POST /api/categories/tree`、`POST /api/categories/tree/query`
- [x] Step1：结果层建表脚本与 Mapper/实体骨架完成
- [x] Step2：预计算任务中心接口骨架完成
- [x] Step3-基线：Spring Batch 最小闭环（单窗口，含 Redis 进度上报）完成
- [ ] Step3-完整版：Spring Batch 五迭代批次进行中

## 四、当前开发清单（功能级）

### 0. 基线与分支
- [x] 建立开发分支 `feature/diagnosis-backend-rewrite`
- [x] 明确后端改写范围与验收口径

### 1. 工程与模块结构
- [x] 新增 `category-analysis-diagnosis` 模块
- [x] 父 `pom.xml` 与 `category-analysis-modules/pom.xml` 完成注册
- [x] 建立 API/应用/基础设施分层目录

### 2. 数据源与配置
- [x] 统一单 SQL Server 数据源（`diagnosis`）
- [x] 预留 Redis key 与 TTL 配置

### 3. 数据库建模（结果层）
- [x] 产出建表脚本：`script/tmp/init_diagnosis_step1_tables_sqlserver.sql`
- [ ] 在 `diagnosis` 库执行建表并验证索引
- [ ] 校验结果表与导出任务表完整性

### 4. 品类树接口
- [x] `POST /api/categories/tree`
- [x] `POST /api/categories/tree/query`
- [ ] `POST /api/categories/filter-options`（仅返回字典项：品类级别/品类角色/在售状态）
- [ ] 取消 `POST /api/categories/tree/children`（待前端确认移除）
- [ ] 与 `plus-ui` 页面联调并验收

### 5. 预计算任务中心
- [x] `POST /api/v1/precompute/jobs`
- [x] `GET /api/v1/precompute/jobs/{jobId}`
- [x] `GET /api/v1/precompute/jobs/{jobId}/progress`
- [x] `GET /api/v1/precompute/jobs/{jobId}/windows`
- [x] `POST /api/v1/precompute/jobs/{jobId}/stop`
- [x] `POST /api/v1/precompute/jobs/{jobId}/retry`

### 6. 模型切换接力看板
- 当前进行中：**Step 5（日期与对比窗口）**

#### Step 1：结果层基础表
- [x] SQL 脚本完成
- [x] Mapper/实体骨架完成
- [ ] SQL Server 落库执行与校验

#### Step 2：任务中心骨架
- [x] 接口骨架完成

#### Step 3：Spring Batch 完整版（五个迭代批次）
- [x] 基线闭环：WindowPlan（单窗口）/Reader 分片/Processor 聚合/Writer 落库/Redis 进度
- [x] 基线闭环：参数传递改为 JobParameters/Batch 上下文（去掉 `System.getProperty`）

迭代批次 1/5：多窗口规划与任务入参升级
- [x] `PrecomputeJobCreateRequest` 增加对比期参数（`compareStart/compareEnd`）
- [x] WindowPlan 支持多窗口类型解析（`MONTH,YOY`）
- [x] 支持 YOY 对比窗口生成（显式对比期或默认去年同期）
- [x] 任务进度 `totalWindows` 按实际窗口数更新
- [x] 多窗口串行调度执行（窗口完成后自动派发下一个 `PENDING`）
- [ ] 多窗口并行调度（后续批次实现）

迭代批次 2/5：作业编排与容错
- [x] 多窗口调度器（按 `PENDING -> RUNNING -> SUCCESS/FAILED` 串行驱动）
- [x] 失败重试与断点续跑（窗口级，非 SUCCESS 窗口重置并重跑）
- [x] 幂等控制与重复提交保护（`requestHash` 命中活动任务直接复用）
- [x] 事件日志标准化（关键阶段补 `payload_json`）
- [ ] 窗口并行调度与全量断点续跑细化（后续批次继续）

迭代批次 3/5：指标扩展与结果层补全
- [x] overview/trends 首版补齐（支持 compareValue/growthRate）
- [x] role-distribution 与 insights 结果表落库（首版骨架）
- [ ] dataVersion 发布机制（草稿版->生效版）
- [ ] 查询侧缓存一致性（版本命中）

迭代批次 4/5：诊断查询 API 完整化
- [x] `POST /api/v1/diagnosis/sessions`
- [x] `GET /api/v1/diagnosis/overview`
- [x] `GET /api/v1/diagnosis/trends`
- [x] 统一返回字段：`requestId/dataVersion/cacheHit`
- [x] 会话触发预计算与短轮询等待（首版）
- [ ] 会话异步通知与状态流转（后续补）

迭代批次 5/5：性能与上线收口
- [ ] 大表索引/分片参数调优与压测
- [x] 监控接口首版（任务事件流查询）
- [x] 停止保护首版（STOPPED 状态下分片/汇总跳过）
- [ ] 告警规则与指标上报（任务时长、失败率、缓存命中）
- [ ] 回归测试清单与灰度发布方案
- [ ] 旧接口下线与前端最终联调验收

#### Step 4：诊断首屏接口（下一步）
- [x] `POST /api/v1/diagnosis/sessions`（首版）
- [x] `GET /api/v1/diagnosis/overview`（首版）
- [x] `GET /api/v1/diagnosis/trends`（首版）
- [x] 返回统一字段：`requestId/dataVersion/cacheHit`（首版）
- [x] 会话驱动预计算触发与等待策略（首版）
- [x] `overview` 响应模型已扩展为 `/classPerformance` 风格字段（首版，仅部分字段已实赋值）
- [ ] `sessions -> queryHash -> batch -> snapshot -> overview` 需补齐门店/品类筛选一致性（进行中）
- [ ] 会话状态查询与异步通知（后续补）

#### Step 4A：库存/周转口径冻结（2026-04-13）
- [x] `avgInventory = 周期内每日库存成本求和 / 天数`
- [x] `inventorySales = avgInventory / sales`
- [x] `turnoverDays = avgInventory / sales * 周期天数`
- [x] `turnoverRate = 动销 SKU / 总 SKU`
- [x] `classSku = 品类下有效状态商品去重数（仅排除停用/作废/下架/无效）`
- [x] `动销SKU = 周期内有销售记录的 SKU`

#### Step 5：日期与对比窗口
- [x] `GET /api/v1/diagnosis/date-ranges`（首版）
- [x] `POST /api/v1/diagnosis/compare-windows/suggest`（首版）
- [x] `POST /api/v1/diagnosis/compare-windows/validate`（首版）
- [ ] 与前端业务规则对齐（节假日/自然周/财务月）

#### Step 6：`classPerformance` 对齐收口（新增）
- [x] 已梳理 `classPerformance` 字段清单及数据库可计算性（`fact_sales_day/fact_stock_day/sh7_saleplu/base_department`）
- [x] 已落代码：`DiagnosisSessionCreateRequest` 增加门店维度字段（`deptId/retailTypeId/businessCircleId/deptGroupId/storeNo`）
- [x] 已落代码：`DiagnosisQueryHashService` 纳入门店/品类/日期/额外过滤参与哈希
- [x] 已落代码：批任务启动参数新增 `requestJson` 透传
- [x] 已落代码：`DiagnosisSourceShardParam` 增加门店/品类过滤字段
- [x] 已落代码：`DiagnosisBatchSourceMapper.xml` 增加门店/品类过滤条件（首版）
- [ ] 待完成：`Finalize/Worker/Partitioner` 过滤解析逻辑与 SQL 口径联调验证
- [ ] 待完成：`overview` 第一批可赋值字段补齐（销量、成本、客数、客单价、渗透率）
- [ ] 待完成：`overview` 第二批库存周转字段补齐（库存额、库销比、周转天数）
- [ ] 待完成：`turnoverRate/classSku` 联调验证并确认商品状态过滤值
- [ ] 待完成：本地编译与回归验证（当前环境缺少 `mvn`）

#### Step 7：`/salesStoreClass/trendChanges` 兼容落地（新增）
- [x] 旧版接口与 Node 参考实现已阅读：确认趋势接口属于“指标计算 + 趋势结果查询”模型
- [x] 口径已冻结：按天返回、当前期/对比期天数必须一致、按索引对齐、`flag=1/2`、两位小数、分母为 0 返回 0
- [x] 关键字段口径已冻结：
- `customerCount = COUNT(DISTINCT sale_no)`
- `grossRate = gross / sales * 100`
- `customerPrice = sales / customerCount`
- `stockCost = 每日库存成本（当天库存金额）`
- `stockCostRate = 库存占用成本 / 销售额 * 100`
- [x] 结果层方案已确定：新增趋势宽表，不复用 `diag_result_trends`
- [x] 已落代码：新增结果表脚本 `script/tmp/003_upgrade_diagnosis_category_performance_trend_sqlserver.sql`
- [x] 已落代码：新增趋势宽表 `diag_result_category_performance_trend`
- [x] 已落代码：新增趋势宽表 Mapper/实体/XML
- [x] 已落代码：新增 batch 按日原料聚合 SQL（销售/销量/毛利/销售成本/客数/库存成本）骨架
- [x] 已落代码：`DiagnosisFinalizeTasklet` 写入当前期/对比期趋势宽表（补零、按索引对齐、两位小数、分母为 0 返回 0）
- [x] 已落代码：新增 trendChanges 查询 service 与 controller（`GET /api/v1/diagnosis/trend-changes`）
- [x] 已落代码：新增旧路径兼容层 `POST /salesStoreClass/trendChanges`
- [ ] 待完成：按旧接口样例逐字段对数与前端联调

## 五、变更日志（按时间追加）

### 2026-04-12 14:30
- 新增“模型切换接力看板”，明确按 Step 推进。
- 新建 Step1 建表脚本：`script/tmp/init_diagnosis_step1_tables_sqlserver.sql`。

### 2026-04-12 15:05
- 完成 Step1 Mapper/实体骨架：
- `DiagnosisPrecomputeJobRow` / `DiagnosisPrecomputeWindowRow` / `DiagnosisPrecomputeEventRow`
- `DiagnosisOverviewSnapshotRow` / `DiagnosisTrendSnapshotRow`
- `DiagnosisPrecomputeMapper` / `DiagnosisSnapshotMapper` 及 XML

### 2026-04-12 15:35
- 完成 Step2 任务中心接口骨架（提交、详情、进度、窗口、停止、重试）。

### 2026-04-12 16:10
- 推进 Step3：完成 Spring Batch 单窗口闭环（分片读取、聚合、落快照）。

### 2026-04-12 16:45
- 补齐 Redis 实时进度上报。
- 进度接口改为“Redis 优先、DB 回退”。
- 批处理参数传递从 `System.getProperty` 切换为 `JobParameters/Batch上下文`。
- 备注：本机仍缺 Maven，尚未完成编译验证。

### 2026-04-12 17:20
- 将 Step3 标题升级为“Spring Batch 完整版（五个迭代批次）”，并写入五批次落地清单。
- 开始迭代批次 1/5 编码：
- `PrecomputeJobCreateRequest` 新增 `compareStart/compareEnd`。
- `DiagnosisWindowPlanService` 从单窗口升级为多窗口规划（`MONTH,YOY`）。
- 任务创建时 `totalWindows` 改为实际窗口数，并启动首个窗口作业。

### 2026-04-12 17:40
- 继续迭代批次 1/5 编码，补齐多窗口串行调度与进度累计：
- `DiagnosisFinalizeTasklet` 改为按 `doneWindows/totalWindows` 累计计算进度。
- `DiagnosisPrecomputeJobListener` 在窗口完成后自动派发下一个 `PENDING` 窗口。
- 多窗口任务由“只跑第一窗”升级为“按窗口顺序串行跑完”。

### 2026-04-12 18:15
- 迭代批次 2/5 已落地首版（编排与容错）：
- 增加幂等哈希：`requestHash`（活动任务命中即复用，避免重复提交）。
- 增加重试字段：`maxRetry/retryCount`（任务级）与 `retryCount`（窗口级）。
- `retryJob` 改为“非 SUCCESS 窗口重置为 PENDING 后重新派发”。
- 关键阶段事件补充 `payload_json`（START/READ/FINALIZE/DISPATCH_NEXT/FAILED）。
- 新增增量脚本：`script/tmp/upgrade_diag_step3_iter2_sqlserver.sql`。

### 2026-04-12 19:00
- 开始迭代批次 3/5，并完成首版落地：
- `FinalizeTasklet` 支持对比窗口聚合，趋势写入 `compareValue/growthRate`。
- `queryHash` 纳入 `compareStart/compareEnd`，避免不同对比窗口冲突。
- 新增扩展结果层 Mapper：`DiagnosisExtendedSnapshotMapper`（角色分布/洞察）。
- 新增结果扩展表增量脚本：`script/tmp/upgrade_diag_step3_iter3_sqlserver.sql`。
- `FinalizeTasklet` 已写入基础 role-distribution 与 insight 示例行。

### 2026-04-12 19:25
- 推进 Step4 首版接口落地：
- 新增 `POST /api/v1/diagnosis/sessions`，基于 queryHash 创建会话并缓存到 Redis。
- 新增 `GET /api/v1/diagnosis/overview`、`GET /api/v1/diagnosis/trends`，读取快照结果。
- 首版响应已统一包含 `requestId`（外层）与 `dataVersion/cacheHit`（内层）。
- 新增迭代 3 增量脚本：`script/tmp/upgrade_diag_step3_iter3_sqlserver.sql`（角色分布/洞察表）。

### 2026-04-12 19:45
- 继续 Step4，补齐会话触发与等待首版：
- `sessions` 支持 `triggerIfMissing/waitSeconds`，结果缺失时自动触发预计算并可短轮询等待。
- `sessions` 增加 `ready/triggeredJobId/source` 字段，区分 `SNAPSHOT_HIT/TRIGGERED/WAIT_READY`。
- `DiagnosisQueryHashService` 调整为与批处理 `queryHash` 一致，避免会话查不到快照。

### 2026-04-12 20:10
- 推进 Step5 首版：
- 新增 `GET /api/v1/diagnosis/date-ranges`。
- 新增 `POST /api/v1/diagnosis/compare-windows/suggest`（支持 YOY/MOM/CUSTOM）。
- 新增 `POST /api/v1/diagnosis/compare-windows/validate`（校验日期完整性与区间天数一致性）。
- 当前已具备“任务触发 -> 快照查询 -> 日期策略”最小业务链路。

### 2026-04-12 20:40
- 推进迭代批次 5/5 首版：
- 任务中心新增事件查询接口：`GET /api/v1/precompute/jobs/{jobId}/events?limit=200`。
- `PrecomputeJobService` 新增 `getJobEvents`，支持 limit 安全阈值（1~1000）。
- 批处理链路新增停止保护：任务状态为 `STOPPED` 时，分片与汇总阶段直接跳过并记录事件。
- 监听器新增停止短路：`STOPPED` 状态下不再派发后续窗口。

### 2026-04-12 21:10
- 按“分步实施（6步）”完成度复盘并记录当前结论：
- 第1步（模型与表）：已完成（100%）。
- 第2步（任务框架）：已完成（95%，已触发真实批处理，不是空流程）。
- 第3步（Reader分片+计算+写入）：已完成（85%，可跑主链路）。
- 第4步（诊断首屏接口）：部分完成（75%，`sessions/overview/trends` 已有，旧参数兼容未完全收口）。
- 第5步（日期与对比窗口能力）：已完成（90%，`date-ranges/suggest/validate` 已落地）。
- 第6步（扩展与收口）：部分完成（30%，`role-distribution/insights` 表与部分写入已具备，API/导出任务化/旧接口兼容层待补齐）。
- 总体进度评估：约 75%~80%，下一阶段优先推进第6步三项收口（扩展查询API、导出任务化、旧接口兼容层）。

### 2026-04-13 14:35
- 对照旧口径接口 `/salesStoreClass/classPerformance` 完成字段级分析，并形成“可直接赋值 / 可计算待接入 / 口径待确认”三类清单。
- `GET /api/v1/diagnosis/overview` 已切换为 `classPerformance` 风格字段输出（首版）；当前仅部分字段有值，其余待批处理扩展。
- 已落代码（待编译验证）：
  - `DiagnosisSessionCreateRequest` 补齐门店范围字段：`deptId/retailTypeId/businessCircleId/deptGroupId/storeNo`
  - `DiagnosisQueryHashService` 纳入门店/品类/日期/extraFilter 生成哈希，避免不同筛选串数据
  - `DiagnosisSessionService` 提交预计算任务时构建结构化 `requestJson` 透传筛选条件
  - `DiagnosisPrecomputeBatchRunner` 支持透传 `requestJson` 到 JobParameters
  - `PrecomputeJobServiceImpl`、`DiagnosisPrecomputeJobListener` 已同步新 `launch` 参数
  - `DiagnosisSourceShardParam` 增加门店/品类过滤字段
  - `DiagnosisBatchSourceMapper.xml` 已加入门店/品类过滤 SQL（首版）
  - `DiagnosisIdRangePartitioner`、`DiagnosisShardWorkerTasklet` 已加入 `requestJson` 解析并下推过滤参数
- 待完成（下一步）：
  - `DiagnosisFinalizeTasklet` 与新版 `queryHash`/过滤口径全链路联调
  - `/overview` 第一批字段补齐：`SaleQuantity/SalesCost/CustomerCount/CustomerPrice/CustomerAvgQuantity/PieceAvgPrice/PenetrateRate`
  - `/overview` 第二批字段补齐：`AvgInventory/InventorySales/TurnoverDays`
  - `turnoverRate` 与 `classSku` 口径确认后落地
  - 本地编译与接口回归（受限：当前环境无 `mvn`）

## 六、Job任务清单（当前状态：已做/缺失）

### 1）立即诊断 -> 会话 -> 提交任务
- [x] `POST /api/v1/diagnosis/sessions` 支持缺快照时触发预计算任务
- [x] 返回 `sessionId`，并缓存 `queryHash/dataVersion`（首版）
- [ ] 查询结果读取链路尚未强制使用 `sessionId + dataVersion` 隔离（当前仍以 `queryHash` 为主，存在串快照风险）

### 2）窗口规划与调度
- [x] 支持 `MONTH/YOY` 窗口规划
- [x] 串行调度下一个窗口（`PENDING -> RUNNING -> SUCCESS/FAILED`）
- [x] 任务停止、窗口重试、事件日志
- [ ] 多窗口并行调度未完成
- [ ] `双月/半年/年` 窗口类型未完成

### 3）分片读取与过滤下推
- [x] 按 ID 范围分片读取
- [x] `requestJson` 透传到 `Partitioner/Worker/Finalize`
- [x] SQL 已支持门店/组织/品类过滤（首版）
- [ ] 门店/品类过滤口径端到端联调验收未完成

### 4）指标计算与落库
- [x] 已计算并落库：`sales/gross/grossRate/totalSku/activeSku/salesRate`
- [x] 趋势已支持：`currentValue/compareValue/growthRate`（首版）
- [ ] `classPerformance` 全字段尚未算完并入库（见第七章）
- [ ] `diag_result_overview` 需补字段并完成 Mapper/实体/写入 SQL 对齐
- [ ] 商品明细结果表（用于 ABC/GMROI/供应商/价格带/品牌/规格分析）尚未建设

### 5）缓存与一致性
- [x] 任务进度 Redis 优先，DB 回退
- [x] 请求哈希已纳入门店/品类/日期/额外过滤（首版）
- [ ] `queryHash` 规范化仍需补齐（空值、默认值、数组排序统一）
- [ ] 快照读取与会话隔离一致性验收未完成

### 6）工程验证
- [ ] 本地缺少 `mvn`，无法完成编译/回归闭环（P0）

## 七、指标计算范围（必须覆盖）

### A. classPerformance 主指标（overview 直接消费）
- [ ] 维度：`classNo/className`
- [ ] SKU：`currentClassSku/compareClassSku/comparativeGrowthRate`
- [ ] 周转率：`currentTurnoverRate/compareTurnoverRate/comparativeTurnoverRate`
- [ ] 渗透率：`currentPenetrateRate/comparePenetrateRate/comparativePenetrateRate`
- [ ] 周转天数：`currentTurnoverDays/compareTurnoverDays/comparativeTurnoverDays`
- [ ] 库销比：`currentInventorySales/compareInventorySales/comparativeInventorySales`
- [ ] 平均库存：`currentAvgInventory/compareAvgInventory/comparativeAvgInventory`
- [ ] 销量：`currentSaleQuantity/compareSaleQuantity/comparativeSaleQuantity`
- [ ] 销售额：`currentSales/compareSales/comparativeSales`
- [ ] 毛利额：`currentGross/compareGross/comparativeGross`
- [ ] 毛利率：`currentGrossRate/compareGrossRate/comparativeGrossRate`
- [ ] 客流：`currentCustomerCount/compareCustomerCount/comparativeCustomerCount`
- [ ] 客单价：`currentCustomerPrice/compareCustomerPrice/comparativeCustomerPrice`
- [ ] 客均件数：`currentCustomerAvgQuantity/compareCustomerAvgQuantity/comparativeCustomerAvgQuantity`
- [ ] 件单价：`currentPieceAvgPrice/comparePieceAvgPrice/comparativePieceAvgPrice`
- [ ] 销售成本：`currentSalesCost/compareSalesCost`
- [ ] 总客流：`currentCustomerCountTotal/compareCustomerCountTotal`

### B. 商品分析域（新增结果层能力）
- [ ] ABC 分析：按销售贡献累计占比进行 A/B/C 分层（商品维度）
- [ ] 毛利分析：商品毛利额、毛利率排行/分布
- [ ] GMROI 分析：`毛利额 / 平均库存成本`
- [ ] 供应商分析：供应商销售、毛利、SKU 贡献、动销贡献
- [ ] 价格带分析：价格区间分桶（销额、销量、毛利、SKU）
- [ ] 品牌分析：品牌销额、毛利、占比、增长
- [ ] 规格分析：规格销额、销量、毛利、结构占比
- [ ] 说明：以上能力无法仅靠 `diag_result_overview/diag_result_trends` 完成，需新增商品明细结果表并落批量写入

### C. 口径待最终确认（阻塞项）
- [x] 客流口径：`sale_no`
- [x] 平均库存口径：期间日均库存成本（每日库存成本平均值）
- [x] `classSku` 规则：有效状态商品去重数；不限制库存/近30天销量/动销/在仓
- [x] `turnoverRate` 规则：动销 SKU / 总 SKU
- [x] `inventorySales` 规则：`avgInventory / sales`
- [x] `turnoverDays` 规则：`avgInventory / sales * 周期天数`
- [ ] 增长率与差值展示规则：百分比（%）还是百分点（pp）逐字段明确

### 2026-04-13 16:20
- 新增“第六章 Job任务清单（已做/缺失）”，用于模型切换后快速接续开发。
- 新增“第七章 指标计算范围（classPerformance + ABC/GMROI/供应商/价格带/品牌/规格）”。
- 明确记录当前关键缺口：
  - 查询读取未完成 `sessionId + dataVersion` 强隔离
  - `overview` 全指标尚未完成计算与落库
  - 商品分析域结果表尚未建设

### 2026-04-13 20:55
- 新增 Step7：`/salesStoreClass/trendChanges` 兼容落地清单。
- 明确 trendChanges 已冻结口径：
  - 当前期按天返回，对比期天数必须与当前期完全一致。
  - 当前期与对比期按索引对齐，不按自然日期映射。
  - `flag` 固定为当前期 `1`、对比期 `2`。
  - 所有数值统一四舍五入保留 2 位小数，分母为 0 时返回 0。
  - `customerCount = COUNT(DISTINCT sale_no)`。
  - `stockCost = 每日库存成本（当天库存金额）`。
  - `stockCostRate = 库存占用成本 / 销售额 * 100`。
- 确认 trendChanges 结果层采用新增宽表方案：`diag_result_category_performance_trend`。

### 2026-04-13 21:20
- 删除独立方案文档 `开发方案-trendChanges接口改造.md`，改为统一维护到总清单。
- 已落代码：新增脚本 `script/tmp/003_upgrade_diagnosis_category_performance_trend_sqlserver.sql`。
- 已落代码：新增趋势宽表实体/Mapper/XML：
  - `DiagnosisCategoryPerformanceTrendRow`
  - `DiagnosisCategoryPerformanceTrendMapper`
  - `DiagnosisCategoryPerformanceTrendMapper.xml`
- 已落代码：扩展 `DiagnosisBatchSourceMapper`，新增按日趋势原料聚合方法：
  - `aggregateDailySalesFacts`
  - `aggregateDailyCustomerCounts`
  - `aggregateDailyStockCosts`
- 当前下一步：继续改造 `DiagnosisFinalizeTasklet`，将当前期/对比期趋势点写入新宽表。

### 2026-04-13 21:40
- 已落代码：`DiagnosisFinalizeTasklet` 已接入趋势宽表写入：
  - 写入当前期 `period_flag = 1` 与对比期 `period_flag = 2`
  - 按日期补齐缺失点并生成 `point_index`
  - 当前期/对比期长度不一致时直接抛错
  - `grossRate/customerPrice/stockCostRate` 统一按口径计算
  - 所有数值统一四舍五入保留 2 位小数，分母为 0 返回 0
- 已同步窗口写入行数统计与事件 `payload`，记录趋势宽表落库行数。
- 当前阻塞仍是本机缺少 `mvn`，暂未完成编译验证。

### 2026-04-13 21:55
- 已落代码：新增趋势查询响应模型：
  - `DiagnosisCategoryPerformanceTrendResponse`
  - `DiagnosisCategoryPerformanceTrendPointResponse`
- 已落代码：`DiagnosisSessionService` 新增 `getTrendChanges(sessionId)`，按 `queryHash + dataVersion` 读取趋势宽表并组装：
  - `xdata`
  - `xdataDB`
  - `lineDate`
- 已落代码：`DiagnosisQueryController` 新增接口 `GET /api/v1/diagnosis/trend-changes?sessionId=...`
- 当前下一步：按旧接口样例逐字段对数，必要时再补旧路径兼容层 `/salesStoreClass/trendChanges`。

### 2026-04-13 22:10
- 已落代码：新增旧版兼容请求/响应模型：
  - `LegacyCategoryTrendChangesRequest`
  - `LegacyCategoryTrendChangesLineResponse`
  - `LegacyCategoryTrendChangesResultResponse`
  - `LegacyNodeResponse`
- 已落代码：新增兼容控制器 `LegacySalesStoreClassController`
  - 对外提供 `POST /salesStoreClass/trendChanges`
  - 旧参数映射到 `DiagnosisSessionCreateRequest`
  - 内部复用 `createSession -> getTrendChanges`
  - 返回旧结构：`success/message/code/result/timestamp`
  - 日期格式转为旧版 `yyyy/MM/dd`
- 当前收尾项：按旧样例逐字段对数，确认 `stockCost/saleCost` 是否允许与旧返回差异。

### 2026-04-13 22:35
- 已冻结库存/周转相关口径：
  - `avgInventory = 周期内每日库存成本平均值`
  - `inventorySales = avgInventory / sales`
  - `turnoverDays = avgInventory / sales * 周期天数`
  - `turnoverRate = 动销 SKU / 总 SKU`
  - `classSku = 有效状态商品去重数`
- 已落代码：`aggregateOverview` 的总 SKU 统计改为从 `base_dept_products` 按有效状态商品去重。
- 已落代码：`overview` 快照模型补齐 `metricCompareTotalSku/metricCompareSalesRate` 映射。
- 已落代码：`DiagnosisFinalizeTasklet` 写入当前/对比期 `classSku` 与 `turnoverRate`。
- 已落代码：`DiagnosisSessionService#getOverview` 返回 `compareClassSku/compareTurnoverRate`。
- 当前待验证项：商品“有效状态”目前按 `status_name not in (停用, 作废, 下架, 无效)` 假设过滤，待数据库实际枚举联调确认。
