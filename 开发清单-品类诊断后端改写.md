# 开发清单-品类诊断后端改写

## 一、项目基本信息
- 项目名称：`category-analysis`（品类诊断后端改写）
- 项目路径：`H:\project_sihuo\new\backend\category-analysis`
- 开发分支：`feature/diagnosis-backend-rewrite`
- 后端技术栈：`Spring Boot + MyBatis-Plus + SQL Server + Redis`
- 唯一数据源：`SQL Server (diagnosis)`
- 配套项目：
- 前端：`H:\project_sihuo\new\ui\plus-ui`
- 旧版参考：`H:\project_sihuo\new\资料\nodejs_code`

## 二、问题记录（持续更新）
### P0（阻塞）
- [ ] 本地环境缺少 `mvn` 命令，无法执行编译与单测验证
- 影响：无法完成本地编译闭环
- 处理：安装 Maven 并加入 `PATH` 后执行：`mvn -pl category-analysis-modules/category-analysis-diagnosis/category-analysis-diagnosis-api -am -DskipTests compile`

### P1（高优先）
- [ ] `POST /api/categories/filter-options` 未最终按字典口径完成联调验收
- [ ] `POST /api/categories/tree/children` 计划取消（后续确认前端是否仍依赖）

### P2（中优先）
- [ ] `class_role`、`skuAbnormal` 业务口径待最终确认

## 三、已完成事项（里程碑）
- [x] 诊断模块基础分层与接口骨架已建立
- [x] 项目收敛为单 SQL Server 数据源（`diagnosis`）
- [x] 品类树接口：`POST /api/categories/tree`、`POST /api/categories/tree/query`
- [x] Step1：结果层建表脚本与 Mapper/实体骨架完成
- [x] Step2：预计算任务中心接口骨架完成
- [x] Step3-基线：Spring Batch 最小闭环（单窗口，含 Redis 进度上报）完成
- [ ] Step3-完整版：Spring Batch 五迭代批次进行中

## 四、当前开发清单（功能级）

### 0. 基线与分支
- [x] 建立开发分支 `feature/diagnosis-backend-rewrite`
- [x] 明确后端改写范围与验收口径

### 1. 工程与模块结构
- [x] 新增 `category-analysis-diagnosis` 模块
- [x] 父 `pom.xml` 与 `category-analysis-modules/pom.xml` 完成注册
- [x] 建立 API/应用/基础设施分层目录

### 2. 数据源与配置
- [x] 统一单 SQL Server 数据源（`diagnosis`）
- [x] 预留 Redis key 与 TTL 配置

### 3. 数据库建模（结果层）
- [x] 产出建表脚本：`script/tmp/init_diagnosis_step1_tables_sqlserver.sql`
- [ ] 在 `diagnosis` 库执行建表并验证索引
- [ ] 校验结果表与导出任务表完整性

### 4. 品类树接口
- [x] `POST /api/categories/tree`
- [x] `POST /api/categories/tree/query`
- [ ] `POST /api/categories/filter-options`（仅返回字典项：品类级别/品类角色/在售状态）
- [ ] 取消 `POST /api/categories/tree/children`（待前端确认移除）
- [ ] 与 `plus-ui` 页面联调并验收

### 5. 预计算任务中心
- [x] `POST /api/v1/precompute/jobs`
- [x] `GET /api/v1/precompute/jobs/{jobId}`
- [x] `GET /api/v1/precompute/jobs/{jobId}/progress`
- [x] `GET /api/v1/precompute/jobs/{jobId}/windows`
- [x] `POST /api/v1/precompute/jobs/{jobId}/stop`
- [x] `POST /api/v1/precompute/jobs/{jobId}/retry`

### 6. 模型切换接力看板
- 当前进行中：**Step 5（日期与对比窗口）**

#### Step 1：结果层基础表
- [x] SQL 脚本完成
- [x] Mapper/实体骨架完成
- [ ] SQL Server 落库执行与校验

#### Step 2：任务中心骨架
- [x] 接口骨架完成

#### Step 3：Spring Batch 完整版（五个迭代批次）
- [x] 基线闭环：WindowPlan（单窗口）/Reader 分片/Processor 聚合/Writer 落库/Redis 进度
- [x] 基线闭环：参数传递改为 JobParameters/Batch 上下文（去掉 `System.getProperty`）

迭代批次 1/5：多窗口规划与任务入参升级
- [x] `PrecomputeJobCreateRequest` 增加对比期参数（`compareStart/compareEnd`）
- [x] WindowPlan 支持多窗口类型解析（`MONTH,YOY`）
- [x] 支持 YOY 对比窗口生成（显式对比期或默认去年同期）
- [x] 任务进度 `totalWindows` 按实际窗口数更新
- [x] 多窗口串行调度执行（窗口完成后自动派发下一个 `PENDING`）
- [ ] 多窗口并行调度（后续批次实现）

迭代批次 2/5：作业编排与容错
- [x] 多窗口调度器（按 `PENDING -> RUNNING -> SUCCESS/FAILED` 串行驱动）
- [x] 失败重试与断点续跑（窗口级，非 SUCCESS 窗口重置并重跑）
- [x] 幂等控制与重复提交保护（`requestHash` 命中活动任务直接复用）
- [x] 事件日志标准化（关键阶段补 `payload_json`）
- [ ] 窗口并行调度与全量断点续跑细化（后续批次继续）

迭代批次 3/5：指标扩展与结果层补全
- [x] overview/trends 首版补齐（支持 compareValue/growthRate）
- [x] role-distribution 与 insights 结果表落库（首版骨架）
- [ ] dataVersion 发布机制（草稿版->生效版）
- [ ] 查询侧缓存一致性（版本命中）

迭代批次 4/5：诊断查询 API 完整化
- [x] `POST /api/v1/diagnosis/sessions`
- [x] `GET /api/v1/diagnosis/overview`
- [x] `GET /api/v1/diagnosis/trends`
- [x] 统一返回字段：`requestId/dataVersion/cacheHit`
- [x] 会话触发预计算与短轮询等待（首版）
- [ ] 会话异步通知与状态流转（后续补）

迭代批次 5/5：性能与上线收口
- [ ] 大表索引/分片参数调优与压测
- [x] 监控接口首版（任务事件流查询）
- [x] 停止保护首版（STOPPED 状态下分片/汇总跳过）
- [ ] 告警规则与指标上报（任务时长、失败率、缓存命中）
- [ ] 回归测试清单与灰度发布方案
- [ ] 旧接口下线与前端最终联调验收

#### Step 4：诊断首屏接口（下一步）
- [x] `POST /api/v1/diagnosis/sessions`（首版）
- [x] `GET /api/v1/diagnosis/overview`（首版）
- [x] `GET /api/v1/diagnosis/trends`（首版）
- [x] 返回统一字段：`requestId/dataVersion/cacheHit`（首版）
- [x] 会话驱动预计算触发与等待策略（首版）
- [ ] 会话状态查询与异步通知（后续补）

#### Step 5：日期与对比窗口
- [x] `GET /api/v1/diagnosis/date-ranges`（首版）
- [x] `POST /api/v1/diagnosis/compare-windows/suggest`（首版）
- [x] `POST /api/v1/diagnosis/compare-windows/validate`（首版）
- [ ] 与前端业务规则对齐（节假日/自然周/财务月）

## 五、变更日志（按时间追加）

### 2026-04-12 14:30
- 新增“模型切换接力看板”，明确按 Step 推进。
- 新建 Step1 建表脚本：`script/tmp/init_diagnosis_step1_tables_sqlserver.sql`。

### 2026-04-12 15:05
- 完成 Step1 Mapper/实体骨架：
- `DiagnosisPrecomputeJobRow` / `DiagnosisPrecomputeWindowRow` / `DiagnosisPrecomputeEventRow`
- `DiagnosisOverviewSnapshotRow` / `DiagnosisTrendSnapshotRow`
- `DiagnosisPrecomputeMapper` / `DiagnosisSnapshotMapper` 及 XML

### 2026-04-12 15:35
- 完成 Step2 任务中心接口骨架（提交、详情、进度、窗口、停止、重试）。

### 2026-04-12 16:10
- 推进 Step3：完成 Spring Batch 单窗口闭环（分片读取、聚合、落快照）。

### 2026-04-12 16:45
- 补齐 Redis 实时进度上报。
- 进度接口改为“Redis 优先、DB 回退”。
- 批处理参数传递从 `System.getProperty` 切换为 `JobParameters/Batch上下文`。
- 备注：本机仍缺 Maven，尚未完成编译验证。

### 2026-04-12 17:20
- 将 Step3 标题升级为“Spring Batch 完整版（五个迭代批次）”，并写入五批次落地清单。
- 开始迭代批次 1/5 编码：
- `PrecomputeJobCreateRequest` 新增 `compareStart/compareEnd`。
- `DiagnosisWindowPlanService` 从单窗口升级为多窗口规划（`MONTH,YOY`）。
- 任务创建时 `totalWindows` 改为实际窗口数，并启动首个窗口作业。

### 2026-04-12 17:40
- 继续迭代批次 1/5 编码，补齐多窗口串行调度与进度累计：
- `DiagnosisFinalizeTasklet` 改为按 `doneWindows/totalWindows` 累计计算进度。
- `DiagnosisPrecomputeJobListener` 在窗口完成后自动派发下一个 `PENDING` 窗口。
- 多窗口任务由“只跑第一窗”升级为“按窗口顺序串行跑完”。

### 2026-04-12 18:15
- 迭代批次 2/5 已落地首版（编排与容错）：
- 增加幂等哈希：`requestHash`（活动任务命中即复用，避免重复提交）。
- 增加重试字段：`maxRetry/retryCount`（任务级）与 `retryCount`（窗口级）。
- `retryJob` 改为“非 SUCCESS 窗口重置为 PENDING 后重新派发”。
- 关键阶段事件补充 `payload_json`（START/READ/FINALIZE/DISPATCH_NEXT/FAILED）。
- 新增增量脚本：`script/tmp/upgrade_diag_step3_iter2_sqlserver.sql`。

### 2026-04-12 19:00
- 开始迭代批次 3/5，并完成首版落地：
- `FinalizeTasklet` 支持对比窗口聚合，趋势写入 `compareValue/growthRate`。
- `queryHash` 纳入 `compareStart/compareEnd`，避免不同对比窗口冲突。
- 新增扩展结果层 Mapper：`DiagnosisExtendedSnapshotMapper`（角色分布/洞察）。
- 新增结果扩展表增量脚本：`script/tmp/upgrade_diag_step3_iter3_sqlserver.sql`。
- `FinalizeTasklet` 已写入基础 role-distribution 与 insight 示例行。

### 2026-04-12 19:25
- 推进 Step4 首版接口落地：
- 新增 `POST /api/v1/diagnosis/sessions`，基于 queryHash 创建会话并缓存到 Redis。
- 新增 `GET /api/v1/diagnosis/overview`、`GET /api/v1/diagnosis/trends`，读取快照结果。
- 首版响应已统一包含 `requestId`（外层）与 `dataVersion/cacheHit`（内层）。
- 新增迭代 3 增量脚本：`script/tmp/upgrade_diag_step3_iter3_sqlserver.sql`（角色分布/洞察表）。

### 2026-04-12 19:45
- 继续 Step4，补齐会话触发与等待首版：
- `sessions` 支持 `triggerIfMissing/waitSeconds`，结果缺失时自动触发预计算并可短轮询等待。
- `sessions` 增加 `ready/triggeredJobId/source` 字段，区分 `SNAPSHOT_HIT/TRIGGERED/WAIT_READY`。
- `DiagnosisQueryHashService` 调整为与批处理 `queryHash` 一致，避免会话查不到快照。

### 2026-04-12 20:10
- 推进 Step5 首版：
- 新增 `GET /api/v1/diagnosis/date-ranges`。
- 新增 `POST /api/v1/diagnosis/compare-windows/suggest`（支持 YOY/MOM/CUSTOM）。
- 新增 `POST /api/v1/diagnosis/compare-windows/validate`（校验日期完整性与区间天数一致性）。
- 当前已具备“任务触发 -> 快照查询 -> 日期策略”最小业务链路。

### 2026-04-12 20:40
- 推进迭代批次 5/5 首版：
- 任务中心新增事件查询接口：`GET /api/v1/precompute/jobs/{jobId}/events?limit=200`。
- `PrecomputeJobService` 新增 `getJobEvents`，支持 limit 安全阈值（1~1000）。
- 批处理链路新增停止保护：任务状态为 `STOPPED` 时，分片与汇总阶段直接跳过并记录事件。
- 监听器新增停止短路：`STOPPED` 状态下不再派发后续窗口。

### 2026-04-12 21:10
- 按“分步实施（6步）”完成度复盘并记录当前结论：
- 第1步（模型与表）：已完成（100%）。
- 第2步（任务框架）：已完成（95%，已触发真实批处理，不是空流程）。
- 第3步（Reader分片+计算+写入）：已完成（85%，可跑主链路）。
- 第4步（诊断首屏接口）：部分完成（75%，`sessions/overview/trends` 已有，旧参数兼容未完全收口）。
- 第5步（日期与对比窗口能力）：已完成（90%，`date-ranges/suggest/validate` 已落地）。
- 第6步（扩展与收口）：部分完成（30%，`role-distribution/insights` 表与部分写入已具备，API/导出任务化/旧接口兼容层待补齐）。
- 总体进度评估：约 75%~80%，下一阶段优先推进第6步三项收口（扩展查询API、导出任务化、旧接口兼容层）。
