<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.diagnosis.infrastructure.mapper.DiagnosisChannelPerformanceMapper">

    <delete id="deleteContributionByVersion">
        DELETE FROM diag_result_channel_contribution
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
    </delete>

    <insert id="batchInsertContribution">
        INSERT INTO diag_result_channel_contribution
        (
            tenant_id, query_hash, data_version,
            sale_channel, online_type, online_name, channel_name,
            retail_type_id, dept_id, business_circle_id, dept_group_id, store_no,
            period_start, period_end, compare_start, compare_end,
            current_sales, current_sales_per, current_gross, current_gross_per, current_gross_rate,
            current_customer_count, current_customer_price,
            compare_sales, compare_sales_per, compare_sales_inc,
            compare_gross, compare_gross_per, compare_gross_inc, compare_gross_rate,
            compare_customer_count, compare_customer_count_inc,
            compare_customer_price, compare_customer_price_inc,
            snapshot_time
        )
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (
                #{row.tenantId}, #{row.queryHash}, #{row.dataVersion},
                #{row.saleChannel}, #{row.onlineType}, #{row.onlineName}, #{row.channelName},
                #{row.retailTypeId}, #{row.deptId}, #{row.businessCircleId}, #{row.deptGroupId}, #{row.storeNo},
                #{row.periodStart}, #{row.periodEnd}, #{row.compareStart}, #{row.compareEnd},
                #{row.currentSales}, #{row.currentSalesPer}, #{row.currentGross}, #{row.currentGrossPer}, #{row.currentGrossRate},
                #{row.currentCustomerCount}, #{row.currentCustomerPrice},
                #{row.compareSales}, #{row.compareSalesPer}, #{row.compareSalesInc},
                #{row.compareGross}, #{row.compareGrossPer}, #{row.compareGrossInc}, #{row.compareGrossRate},
                #{row.compareCustomerCount}, #{row.compareCustomerCountInc},
                #{row.compareCustomerPrice}, #{row.compareCustomerPriceInc},
                COALESCE(#{row.snapshotTime}, SYSUTCDATETIME())
            )
        </foreach>
    </insert>

    <select id="selectContributionByVersion" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisChannelContributionRow">
        SELECT
            id,
            tenant_id AS tenantId,
            query_hash AS queryHash,
            data_version AS dataVersion,
            sale_channel AS saleChannel,
            online_type AS onlineType,
            online_name AS onlineName,
            channel_name AS channelName,
            retail_type_id AS retailTypeId,
            dept_id AS deptId,
            business_circle_id AS businessCircleId,
            dept_group_id AS deptGroupId,
            store_no AS storeNo,
            period_start AS periodStart,
            period_end AS periodEnd,
            compare_start AS compareStart,
            compare_end AS compareEnd,
            current_sales AS currentSales,
            current_sales_per AS currentSalesPer,
            current_gross AS currentGross,
            current_gross_per AS currentGrossPer,
            current_gross_rate AS currentGrossRate,
            current_customer_count AS currentCustomerCount,
            current_customer_price AS currentCustomerPrice,
            compare_sales AS compareSales,
            compare_sales_per AS compareSalesPer,
            compare_sales_inc AS compareSalesInc,
            compare_gross AS compareGross,
            compare_gross_per AS compareGrossPer,
            compare_gross_inc AS compareGrossInc,
            compare_gross_rate AS compareGrossRate,
            compare_customer_count AS compareCustomerCount,
            compare_customer_count_inc AS compareCustomerCountInc,
            compare_customer_price AS compareCustomerPrice,
            compare_customer_price_inc AS compareCustomerPriceInc,
            snapshot_time AS snapshotTime,
            create_time AS createTime,
            update_time AS updateTime
        FROM diag_result_channel_contribution
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
        ORDER BY sale_channel ASC, online_type ASC, id ASC
    </select>

    <select id="selectContributionPageByVersion" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisChannelContributionRow">
        SELECT
            id,
            tenant_id AS tenantId,
            query_hash AS queryHash,
            data_version AS dataVersion,
            sale_channel AS saleChannel,
            online_type AS onlineType,
            online_name AS onlineName,
            channel_name AS channelName,
            retail_type_id AS retailTypeId,
            dept_id AS deptId,
            business_circle_id AS businessCircleId,
            dept_group_id AS deptGroupId,
            store_no AS storeNo,
            period_start AS periodStart,
            period_end AS periodEnd,
            compare_start AS compareStart,
            compare_end AS compareEnd,
            current_sales AS currentSales,
            current_sales_per AS currentSalesPer,
            current_gross AS currentGross,
            current_gross_per AS currentGrossPer,
            current_gross_rate AS currentGrossRate,
            current_customer_count AS currentCustomerCount,
            current_customer_price AS currentCustomerPrice,
            compare_sales AS compareSales,
            compare_sales_per AS compareSalesPer,
            compare_sales_inc AS compareSalesInc,
            compare_gross AS compareGross,
            compare_gross_per AS compareGrossPer,
            compare_gross_inc AS compareGrossInc,
            compare_gross_rate AS compareGrossRate,
            compare_customer_count AS compareCustomerCount,
            compare_customer_count_inc AS compareCustomerCountInc,
            compare_customer_price AS compareCustomerPrice,
            compare_customer_price_inc AS compareCustomerPriceInc,
            snapshot_time AS snapshotTime,
            create_time AS createTime,
            update_time AS updateTime
        FROM diag_result_channel_contribution
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
        ORDER BY ${orderBy} ${orderType}, sale_channel ASC, online_type ASC, id ASC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
    </select>

    <select id="countContributionByVersion" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM diag_result_channel_contribution
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
    </select>

    <delete id="deleteTrendByVersion">
        DELETE FROM diag_result_channel_trend
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
    </delete>

    <insert id="batchInsertTrend">
        INSERT INTO diag_result_channel_trend
        (
            tenant_id, query_hash, data_version,
            sale_channel, online_type, online_name, channel_name,
            retail_type_id, dept_id, business_circle_id, dept_group_id, store_no,
            point_index, point_date, current_sales, snapshot_time
        )
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (
                #{row.tenantId}, #{row.queryHash}, #{row.dataVersion},
                #{row.saleChannel}, #{row.onlineType}, #{row.onlineName}, #{row.channelName},
                #{row.retailTypeId}, #{row.deptId}, #{row.businessCircleId}, #{row.deptGroupId}, #{row.storeNo},
                #{row.pointIndex}, #{row.pointDate}, #{row.currentSales},
                COALESCE(#{row.snapshotTime}, SYSUTCDATETIME())
            )
        </foreach>
    </insert>

    <select id="selectTrendByVersion" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisChannelTrendRow">
        SELECT
            id,
            tenant_id AS tenantId,
            query_hash AS queryHash,
            data_version AS dataVersion,
            sale_channel AS saleChannel,
            online_type AS onlineType,
            online_name AS onlineName,
            channel_name AS channelName,
            retail_type_id AS retailTypeId,
            dept_id AS deptId,
            business_circle_id AS businessCircleId,
            dept_group_id AS deptGroupId,
            store_no AS storeNo,
            point_index AS pointIndex,
            point_date AS pointDate,
            current_sales AS currentSales,
            snapshot_time AS snapshotTime,
            create_time AS createTime,
            update_time AS updateTime
        FROM diag_result_channel_trend
        WHERE tenant_id = #{tenantId}
          AND query_hash = #{queryHash}
          AND data_version = #{dataVersion}
        ORDER BY point_index ASC, sale_channel ASC, online_type ASC, id ASC
    </select>

</mapper>
