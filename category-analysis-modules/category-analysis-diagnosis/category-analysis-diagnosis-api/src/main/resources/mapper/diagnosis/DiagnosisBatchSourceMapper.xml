<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.diagnosis.infrastructure.mapper.DiagnosisBatchSourceMapper">

    <sql id="classFilterSales">
        <if test="param.classNo != null and param.classNo != '' and param.classNo != '0'">
            <choose>
                <when test="param.classLevel == 1">
                    AND one_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 2">
                    AND two_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 3">
                    AND three_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 4">
                    AND four_class_no = #{param.classNo}
                </when>
                <otherwise>
                    AND (
                        one_class_no = #{param.classNo}
                        OR two_class_no = #{param.classNo}
                        OR three_class_no = #{param.classNo}
                        OR four_class_no = #{param.classNo}
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <sql id="storeFilterSales">
        <if test="param.storeNo != null and param.storeNo != '' and param.storeNo != '0' and param.storeNo != 'all'">
            AND store_no = #{param.storeNo}
        </if>
        <if test="(param.storeNo == null or param.storeNo == '' or param.storeNo == '0' or param.storeNo == 'all')
            and (
                (param.deptId != null and param.deptId != '' and param.deptId != '0')
                or (param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0')
                or (param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0')
                or (param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0')
            )">
            AND EXISTS (
                SELECT 1
                FROM base_department bd
                WHERE bd.store_no = fact_sales_day.store_no
                  AND bd.status_no = 1
                  AND bd.store_type_no = '1'
                <if test="param.deptId != null and param.deptId != '' and param.deptId != '0'">
                    AND bd.preorgcode = #{param.deptId}
                </if>
                <if test="param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0'">
                    AND bd.store_format_no = #{param.retailTypeId}
                </if>
                <if test="param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0'">
                    AND bd.business_circle_no = #{param.businessCircleId}
                </if>
                <if test="param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0'">
                    AND bd.store_group_no = #{param.deptGroupId}
                </if>
            )
        </if>
    </sql>

    <sql id="classFilterStock">
        <if test="param.classNo != null and param.classNo != '' and param.classNo != '0'">
            <choose>
                <when test="param.classLevel == 1">
                    AND one_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 2">
                    AND two_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 3">
                    AND three_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 4">
                    AND four_class_no = #{param.classNo}
                </when>
                <otherwise>
                    AND (
                        one_class_no = #{param.classNo}
                        OR two_class_no = #{param.classNo}
                        OR three_class_no = #{param.classNo}
                        OR four_class_no = #{param.classNo}
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <sql id="storeFilterStock">
        <if test="param.storeNo != null and param.storeNo != '' and param.storeNo != '0' and param.storeNo != 'all'">
            AND store_no = #{param.storeNo}
        </if>
        <if test="(param.storeNo == null or param.storeNo == '' or param.storeNo == '0' or param.storeNo == 'all')
            and (
                (param.deptId != null and param.deptId != '' and param.deptId != '0')
                or (param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0')
                or (param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0')
                or (param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0')
            )">
            AND EXISTS (
                SELECT 1
                FROM base_department bd
                WHERE bd.store_no = fact_stock_day.store_no
                  AND bd.status_no = 1
                  AND bd.store_type_no = '1'
                <if test="param.deptId != null and param.deptId != '' and param.deptId != '0'">
                    AND bd.preorgcode = #{param.deptId}
                </if>
                <if test="param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0'">
                    AND bd.store_format_no = #{param.retailTypeId}
                </if>
                <if test="param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0'">
                    AND bd.business_circle_no = #{param.businessCircleId}
                </if>
                <if test="param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0'">
                    AND bd.store_group_no = #{param.deptGroupId}
                </if>
            )
        </if>
    </sql>

    <sql id="classFilterSh7">
        <if test="param.classNo != null and param.classNo != '' and param.classNo != '0'">
            <choose>
                <when test="param.classLevel == 1">
                    AND one_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 2">
                    AND two_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 3">
                    AND three_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 4">
                    AND four_class_no = #{param.classNo}
                </when>
                <otherwise>
                    AND (
                        one_class_no = #{param.classNo}
                        OR two_class_no = #{param.classNo}
                        OR three_class_no = #{param.classNo}
                        OR four_class_no = #{param.classNo}
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <sql id="storeFilterSh7">
        <if test="param.storeNo != null and param.storeNo != '' and param.storeNo != '0' and param.storeNo != 'all'">
            AND store_no = #{param.storeNo}
        </if>
        <if test="(param.storeNo == null or param.storeNo == '' or param.storeNo == '0' or param.storeNo == 'all')
            and (
                (param.deptId != null and param.deptId != '' and param.deptId != '0')
                or (param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0')
                or (param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0')
                or (param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0')
            )">
            AND EXISTS (
                SELECT 1
                FROM base_department bd
                WHERE bd.store_no = sh7_saleplu.store_no
                  AND bd.status_no = 1
                  AND bd.store_type_no = '1'
                <if test="param.deptId != null and param.deptId != '' and param.deptId != '0'">
                    AND bd.preorgcode = #{param.deptId}
                </if>
                <if test="param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0'">
                    AND bd.store_format_no = #{param.retailTypeId}
                </if>
                <if test="param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0'">
                    AND bd.business_circle_no = #{param.businessCircleId}
                </if>
                <if test="param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0'">
                    AND bd.store_group_no = #{param.deptGroupId}
                </if>
            )
        </if>
    </sql>

    <sql id="classFilterDeptProducts">
        <if test="param.classNo != null and param.classNo != '' and param.classNo != '0'">
            <choose>
                <when test="param.classLevel == 1">
                    AND p.one_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 2">
                    AND p.two_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 3">
                    AND p.three_class_no = #{param.classNo}
                </when>
                <when test="param.classLevel == 4">
                    AND p.four_class_no = #{param.classNo}
                </when>
                <otherwise>
                    AND (
                        p.one_class_no = #{param.classNo}
                        OR p.two_class_no = #{param.classNo}
                        OR p.three_class_no = #{param.classNo}
                        OR p.four_class_no = #{param.classNo}
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <sql id="storeFilterDeptProducts">
        <if test="param.storeNo != null and param.storeNo != '' and param.storeNo != '0' and param.storeNo != 'all'">
            AND p.store_no = #{param.storeNo}
        </if>
        <if test="(param.storeNo == null or param.storeNo == '' or param.storeNo == '0' or param.storeNo == 'all')
            and (
                (param.deptId != null and param.deptId != '' and param.deptId != '0')
                or (param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0')
                or (param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0')
                or (param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0')
            )">
            AND EXISTS (
                SELECT 1
                FROM base_department bd
                WHERE bd.store_no = p.store_no
                  AND bd.status_no = 1
                  AND bd.store_type_no = '1'
                <if test="param.deptId != null and param.deptId != '' and param.deptId != '0'">
                    AND bd.preorgcode = #{param.deptId}
                </if>
                <if test="param.retailTypeId != null and param.retailTypeId != '' and param.retailTypeId != '0'">
                    AND bd.store_format_no = #{param.retailTypeId}
                </if>
                <if test="param.businessCircleId != null and param.businessCircleId != '' and param.businessCircleId != '0'">
                    AND bd.business_circle_no = #{param.businessCircleId}
                </if>
                <if test="param.deptGroupId != null and param.deptGroupId != '' and param.deptGroupId != '0'">
                    AND bd.store_group_no = #{param.deptGroupId}
                </if>
            )
        </if>
    </sql>

    <sql id="baseWhere">
        WHERE sale_date &gt;= #{param.periodStart}
          AND sale_date &lt;= #{param.periodEnd}
        <include refid="classFilterSales"/>
        <include refid="storeFilterSales"/>
        <if test="param.startId != null">
            AND id &gt;= #{param.startId}
        </if>
        <if test="param.endId != null">
            AND id &lt;= #{param.endId}
        </if>
    </sql>

    <sql id="requireDirectSubclassSales">
        <choose>
            <when test="param.classLevel == 1 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND one_class_no = #{param.classNo}
                AND two_class_no IS NOT NULL AND LTRIM(RTRIM(two_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 2 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND two_class_no = #{param.classNo}
                AND three_class_no IS NOT NULL AND LTRIM(RTRIM(three_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 3 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND three_class_no = #{param.classNo}
                AND four_class_no IS NOT NULL AND LTRIM(RTRIM(four_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 4 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND four_class_no = #{param.classNo}
                AND five_class_no IS NOT NULL AND LTRIM(RTRIM(five_class_no)) &lt;&gt; ''
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
    </sql>

    <sql id="requireDirectSubclassStock">
        <choose>
            <when test="param.classLevel == 1 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND one_class_no = #{param.classNo}
                AND two_class_no IS NOT NULL AND LTRIM(RTRIM(two_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 2 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND two_class_no = #{param.classNo}
                AND three_class_no IS NOT NULL AND LTRIM(RTRIM(three_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 3 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND three_class_no = #{param.classNo}
                AND four_class_no IS NOT NULL AND LTRIM(RTRIM(four_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 4 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND four_class_no = #{param.classNo}
                AND five_class_no IS NOT NULL AND LTRIM(RTRIM(five_class_no)) &lt;&gt; ''
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
    </sql>

    <sql id="requireDirectSubclassSh7">
        <choose>
            <when test="param.classLevel == 1 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND one_class_no = #{param.classNo}
                AND two_class_no IS NOT NULL AND LTRIM(RTRIM(two_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 2 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND two_class_no = #{param.classNo}
                AND three_class_no IS NOT NULL AND LTRIM(RTRIM(three_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 3 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND three_class_no = #{param.classNo}
                AND four_class_no IS NOT NULL AND LTRIM(RTRIM(four_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 4 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND four_class_no = #{param.classNo}
                AND five_class_no IS NOT NULL AND LTRIM(RTRIM(five_class_no)) &lt;&gt; ''
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
    </sql>

    <sql id="requireDirectSubclassDeptProducts">
        <choose>
            <when test="param.classLevel == 1 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND p.one_class_no = #{param.classNo}
                AND p.two_class_no IS NOT NULL AND LTRIM(RTRIM(p.two_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 2 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND p.two_class_no = #{param.classNo}
                AND p.three_class_no IS NOT NULL AND LTRIM(RTRIM(p.three_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 3 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND p.three_class_no = #{param.classNo}
                AND p.four_class_no IS NOT NULL AND LTRIM(RTRIM(p.four_class_no)) &lt;&gt; ''
            </when>
            <when test="param.classLevel == 4 and param.classNo != null and param.classNo != '' and param.classNo != '0'">
                AND p.four_class_no = #{param.classNo}
                AND p.five_class_no IS NOT NULL AND LTRIM(RTRIM(p.five_class_no)) &lt;&gt; ''
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
    </sql>

    <sql id="directSubclassSelectSales">
        <choose>
            <when test="param.classLevel == 1">
                2 AS subClassLevel,
                two_class_no AS subClassNo,
                two_class_name AS subClassName
            </when>
            <when test="param.classLevel == 2">
                3 AS subClassLevel,
                three_class_no AS subClassNo,
                three_class_name AS subClassName
            </when>
            <when test="param.classLevel == 3">
                4 AS subClassLevel,
                four_class_no AS subClassNo,
                four_class_name AS subClassName
            </when>
            <when test="param.classLevel == 4">
                5 AS subClassLevel,
                five_class_no AS subClassNo,
                five_class_name AS subClassName
            </when>
        </choose>
    </sql>

    <sql id="directSubclassSelectStock">
        <choose>
            <when test="param.classLevel == 1">
                2 AS subClassLevel,
                two_class_no AS subClassNo,
                two_class_name AS subClassName
            </when>
            <when test="param.classLevel == 2">
                3 AS subClassLevel,
                three_class_no AS subClassNo,
                three_class_name AS subClassName
            </when>
            <when test="param.classLevel == 3">
                4 AS subClassLevel,
                four_class_no AS subClassNo,
                four_class_name AS subClassName
            </when>
            <when test="param.classLevel == 4">
                5 AS subClassLevel,
                five_class_no AS subClassNo,
                five_class_name AS subClassName
            </when>
        </choose>
    </sql>

    <sql id="directSubclassSelectSh7">
        <choose>
            <when test="param.classLevel == 1">
                2 AS subClassLevel,
                two_class_no AS subClassNo,
                two_class_name AS subClassName
            </when>
            <when test="param.classLevel == 2">
                3 AS subClassLevel,
                three_class_no AS subClassNo,
                three_class_name AS subClassName
            </when>
            <when test="param.classLevel == 3">
                4 AS subClassLevel,
                four_class_no AS subClassNo,
                four_class_name AS subClassName
            </when>
            <when test="param.classLevel == 4">
                5 AS subClassLevel,
                five_class_no AS subClassNo,
                five_class_name AS subClassName
            </when>
        </choose>
    </sql>

    <sql id="directSubclassSelectDeptProducts">
        <choose>
            <when test="param.classLevel == 1">
                2 AS subClassLevel,
                p.two_class_no AS subClassNo,
                p.two_class_name AS subClassName
            </when>
            <when test="param.classLevel == 2">
                3 AS subClassLevel,
                p.three_class_no AS subClassNo,
                p.three_class_name AS subClassName
            </when>
            <when test="param.classLevel == 3">
                4 AS subClassLevel,
                p.four_class_no AS subClassNo,
                p.four_class_name AS subClassName
            </when>
            <when test="param.classLevel == 4">
                5 AS subClassLevel,
                p.five_class_no AS subClassNo,
                p.five_class_name AS subClassName
            </when>
        </choose>
    </sql>

    <select id="selectIdRange" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceIdRangeRow">
        SELECT
            MIN(CAST(id AS BIGINT)) AS minId,
            MAX(CAST(id AS BIGINT)) AS maxId
        FROM fact_sales_day
        <include refid="baseWhere"/>
    </select>

    <select id="countRows" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM fact_sales_day
        <include refid="baseWhere"/>
    </select>

    <select id="aggregateOverview" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceOverviewAggRow">
        SELECT
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales,
            ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS totalGross,
            ISNULL(SUM(CAST(sale_quantity AS DECIMAL(20, 6))), 0) AS totalSaleQuantity,
            ISNULL(SUM(CAST(sale_cost AS DECIMAL(20, 6))), 0) AS totalSalesCost,
            (
                SELECT CAST(COUNT(DISTINCT p.product_no) AS INT)
                FROM base_dept_products p
                WHERE p.product_no IS NOT NULL
                  AND LTRIM(RTRIM(p.product_no)) &lt;&gt; ''
                  <include refid="classFilterDeptProducts"/>
                  <include refid="storeFilterDeptProducts"/>
                  AND ISNULL(LTRIM(RTRIM(p.status_name)), '') NOT IN (N'停用', N'作废', N'下架', N'无效')
            ) AS totalSku,
            CAST(COUNT(DISTINCT CASE WHEN sales &gt; 0 THEN product_no ELSE NULL END) AS INT) AS activeSku
        FROM fact_sales_day
        <include refid="baseWhere"/>
    </select>

    <select id="selectAvgInventory" resultType="java.math.BigDecimal">
        WITH daily_stock AS (
            SELECT
                stock_date,
                ISNULL(SUM(CAST(stock_sale_money AS DECIMAL(20, 6))), 0) AS dayStockSaleMoney
            FROM fact_stock_day
            WHERE stock_date &gt;= #{param.periodStart}
              AND stock_date &lt;= #{param.periodEnd}
              <include refid="classFilterStock"/>
              <include refid="storeFilterStock"/>
            GROUP BY stock_date
        )
        SELECT ISNULL(AVG(CAST(dayStockSaleMoney AS DECIMAL(20, 6))), 0)
        FROM daily_stock
    </select>

    <select id="countCustomerByClass" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT sale_no)
        FROM sh7_saleplu
        WHERE CAST(xsdate AS DATE) &gt;= #{param.periodStart}
          AND CAST(xsdate AS DATE) &lt;= #{param.periodEnd}
          <include refid="classFilterSh7"/>
          <include refid="storeFilterSh7"/>
    </select>

    <select id="countCustomerTotal" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT sale_no)
        FROM sh7_saleplu
        WHERE CAST(xsdate AS DATE) &gt;= #{param.periodStart}
          AND CAST(xsdate AS DATE) &lt;= #{param.periodEnd}
          <include refid="storeFilterSh7"/>
    </select>

    <select id="aggregateTrendsByDate" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceTrendAggRow">
        SELECT
            sale_date AS saleDate,
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales,
            ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS totalGross
        FROM fact_sales_day
        <include refid="baseWhere"/>
        GROUP BY sale_date
        ORDER BY sale_date ASC
    </select>

    <select id="aggregateDailySalesFacts" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceDailyTrendRow">
        SELECT
            sale_date AS pointDate,
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales,
            ISNULL(SUM(CAST(sale_quantity AS DECIMAL(20, 6))), 0) AS totalSaleQuantity,
            ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS totalGross,
            ISNULL(SUM(CAST(sale_cost AS DECIMAL(20, 6))), 0) AS totalSaleCost
        FROM fact_sales_day
        <include refid="baseWhere"/>
        GROUP BY sale_date
        ORDER BY sale_date ASC
    </select>

    <select id="aggregateDailyCustomerCounts" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceDailyTrendRow">
        SELECT
            CAST(xsdate AS DATE) AS pointDate,
            COUNT(DISTINCT sale_no) AS totalCustomerCount
        FROM sh7_saleplu
        WHERE CAST(xsdate AS DATE) &gt;= #{param.periodStart}
          AND CAST(xsdate AS DATE) &lt;= #{param.periodEnd}
          <include refid="classFilterSh7"/>
          <include refid="storeFilterSh7"/>
        GROUP BY CAST(xsdate AS DATE)
        ORDER BY CAST(xsdate AS DATE) ASC
    </select>

    <select id="aggregateDailyStockCosts" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceDailyTrendRow">
        SELECT
            stock_date AS pointDate,
            ISNULL(SUM(CAST(stock_sale_money AS DECIMAL(20, 6))), 0) AS totalStockCost
        FROM fact_stock_day
        WHERE stock_date &gt;= #{param.periodStart}
          AND stock_date &lt;= #{param.periodEnd}
          <include refid="classFilterStock"/>
          <include refid="storeFilterStock"/>
        GROUP BY stock_date
        ORDER BY stock_date ASC
    </select>

    <select id="aggregateSubclassContribution" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceSubclassContributionAggRow">
        WITH sales_agg AS (
            SELECT
                <include refid="directSubclassSelectSales"/>,
                ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales,
                ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS totalGross,
                ISNULL(SUM(CAST(sale_quantity AS DECIMAL(20, 6))), 0) AS totalSaleQuantity,
                ISNULL(SUM(CAST(sale_cost AS DECIMAL(20, 6))), 0) AS totalSaleCost,
                CAST(COUNT(DISTINCT CASE WHEN sales &gt; 0 THEN product_no ELSE NULL END) AS INT) AS activeSku
            FROM fact_sales_day
            <include refid="baseWhere"/>
            <include refid="requireDirectSubclassSales"/>
            GROUP BY
            <choose>
                <when test="param.classLevel == 1">two_class_no, two_class_name</when>
                <when test="param.classLevel == 2">three_class_no, three_class_name</when>
                <when test="param.classLevel == 3">four_class_no, four_class_name</when>
                <when test="param.classLevel == 4">five_class_no, five_class_name</when>
            </choose>
        ),
        customer_agg AS (
            SELECT
                <include refid="directSubclassSelectSh7"/>,
                COUNT(DISTINCT sale_no) AS totalCustomerCount
            FROM sh7_saleplu
            WHERE CAST(xsdate AS DATE) &gt;= #{param.periodStart}
              AND CAST(xsdate AS DATE) &lt;= #{param.periodEnd}
              <include refid="storeFilterSh7"/>
              <include refid="requireDirectSubclassSh7"/>
            GROUP BY
            <choose>
                <when test="param.classLevel == 1">two_class_no, two_class_name</when>
                <when test="param.classLevel == 2">three_class_no, three_class_name</when>
                <when test="param.classLevel == 3">four_class_no, four_class_name</when>
                <when test="param.classLevel == 4">five_class_no, five_class_name</when>
            </choose>
        ),
        inventory_daily AS (
            SELECT
                stock_date,
                <include refid="directSubclassSelectStock"/>,
                ISNULL(SUM(CAST(stock_sale_money AS DECIMAL(20, 6))), 0) AS dayStockSaleMoney
            FROM fact_stock_day
            WHERE stock_date &gt;= #{param.periodStart}
              AND stock_date &lt;= #{param.periodEnd}
              <include refid="storeFilterStock"/>
              <include refid="requireDirectSubclassStock"/>
            GROUP BY stock_date,
            <choose>
                <when test="param.classLevel == 1">two_class_no, two_class_name</when>
                <when test="param.classLevel == 2">three_class_no, three_class_name</when>
                <when test="param.classLevel == 3">four_class_no, four_class_name</when>
                <when test="param.classLevel == 4">five_class_no, five_class_name</when>
            </choose>
        ),
        inventory_agg AS (
            SELECT
                subClassLevel,
                subClassNo,
                MAX(subClassName) AS subClassName,
                ISNULL(AVG(CAST(dayStockSaleMoney AS DECIMAL(20, 6))), 0) AS avgInventory
            FROM inventory_daily
            GROUP BY subClassLevel, subClassNo
        ),
        sku_agg AS (
            SELECT
                <include refid="directSubclassSelectDeptProducts"/>,
                CAST(COUNT(DISTINCT p.product_no) AS INT) AS totalSku
            FROM base_dept_products p
            WHERE p.product_no IS NOT NULL
              AND LTRIM(RTRIM(p.product_no)) &lt;&gt; ''
              <include refid="storeFilterDeptProducts"/>
              <include refid="requireDirectSubclassDeptProducts"/>
              AND ISNULL(LTRIM(RTRIM(p.status_name)), '') NOT IN (N'停用', N'作废', N'下架', N'无效')
            GROUP BY
            <choose>
                <when test="param.classLevel == 1">p.two_class_no, p.two_class_name</when>
                <when test="param.classLevel == 2">p.three_class_no, p.three_class_name</when>
                <when test="param.classLevel == 3">p.four_class_no, p.four_class_name</when>
                <when test="param.classLevel == 4">p.five_class_no, p.five_class_name</when>
            </choose>
        )
        SELECT
            s.subClassLevel,
            s.subClassNo,
            s.subClassName,
            s.totalSales,
            s.totalGross,
            s.totalSaleQuantity,
            s.totalSaleCost,
            ISNULL(c.totalCustomerCount, 0) AS totalCustomerCount,
            ISNULL(i.avgInventory, 0) AS avgInventory,
            ISNULL(k.totalSku, 0) AS totalSku,
            ISNULL(s.activeSku, 0) AS activeSku
        FROM sales_agg s
        LEFT JOIN customer_agg c
          ON c.subClassLevel = s.subClassLevel
         AND c.subClassNo = s.subClassNo
        LEFT JOIN inventory_agg i
          ON i.subClassLevel = s.subClassLevel
         AND i.subClassNo = s.subClassNo
        LEFT JOIN sku_agg k
          ON k.subClassLevel = s.subClassLevel
         AND k.subClassNo = s.subClassNo
        ORDER BY s.subClassNo ASC
    </select>

    <select id="aggregateSubclassDailySales" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceSubclassDailyTrendAggRow">
        SELECT
            <include refid="directSubclassSelectSales"/>,
            sale_date AS pointDate,
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales
        FROM fact_sales_day
        <include refid="baseWhere"/>
        <include refid="requireDirectSubclassSales"/>
        GROUP BY sale_date,
        <choose>
            <when test="param.classLevel == 1">two_class_no, two_class_name</when>
            <when test="param.classLevel == 2">three_class_no, three_class_name</when>
            <when test="param.classLevel == 3">four_class_no, four_class_name</when>
            <when test="param.classLevel == 4">five_class_no, five_class_name</when>
        </choose>
        ORDER BY pointDate ASC, subClassNo ASC
    </select>

    <select id="aggregateChannelContribution" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceChannelContributionAggRow">
        WITH sales_agg AS (
            SELECT
                sale_channel AS saleChannel,
                CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END AS onlineType,
                MAX(NULLIF(LTRIM(RTRIM(online_name)), '')) AS onlineName,
                ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales,
                ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS totalGross
            FROM fact_sales_day
            WHERE sale_date &gt;= #{param.periodStart}
              AND sale_date &lt;= #{param.periodEnd}
              <include refid="classFilterSales"/>
              <include refid="storeFilterSales"/>
            GROUP BY sale_channel,
                CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END
        ),
        customer_agg AS (
            SELECT
                sale_channel AS saleChannel,
                CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END AS onlineType,
                MAX(NULLIF(LTRIM(RTRIM(online_name)), '')) AS onlineName,
                COUNT(DISTINCT sale_no) AS totalCustomerCount
            FROM sh7_saleplu
            WHERE CAST(xsdate AS DATE) &gt;= #{param.periodStart}
              AND CAST(xsdate AS DATE) &lt;= #{param.periodEnd}
              <include refid="classFilterSh7"/>
              <include refid="storeFilterSh7"/>
            GROUP BY sale_channel,
                CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END
        )
        SELECT
            COALESCE(s.saleChannel, c.saleChannel) AS saleChannel,
            COALESCE(s.onlineType, c.onlineType) AS onlineType,
            COALESCE(s.onlineName, c.onlineName) AS onlineName,
            ISNULL(s.totalSales, 0) AS totalSales,
            ISNULL(s.totalGross, 0) AS totalGross,
            ISNULL(CAST(c.totalCustomerCount AS DECIMAL(20, 2)), 0) AS totalCustomerCount
        FROM sales_agg s
        FULL OUTER JOIN customer_agg c
          ON c.saleChannel = s.saleChannel
         AND ISNULL(c.onlineType, '') = ISNULL(s.onlineType, '')
        ORDER BY COALESCE(s.saleChannel, c.saleChannel), COALESCE(s.onlineType, c.onlineType)
    </select>

    <select id="aggregateChannelDailySales" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceChannelDailyTrendAggRow">
        SELECT
            sale_date AS pointDate,
            sale_channel AS saleChannel,
            CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END AS onlineType,
            MAX(NULLIF(LTRIM(RTRIM(online_name)), '')) AS onlineName,
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS totalSales
        FROM fact_sales_day
        WHERE sale_date &gt;= #{param.periodStart}
          AND sale_date &lt;= #{param.periodEnd}
          <include refid="classFilterSales"/>
          <include refid="storeFilterSales"/>
        GROUP BY sale_date, sale_channel,
            CASE WHEN sale_channel = 1 THEN NULL ELSE NULLIF(LTRIM(RTRIM(online_type)), '') END
        ORDER BY pointDate ASC, saleChannel ASC, onlineType ASC
    </select>

    <select id="aggregateVipByGenderAndAge" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceVipGenderAgeAggRow">
        WITH base_data AS (
            SELECT
                CASE
                    WHEN v.gender IN (1, 2) THEN v.gender
                    ELSE 3
                END AS gender,
                CASE
                    WHEN v.birthday IS NOT NULL THEN
                        DATEDIFF(YEAR, v.birthday, #{referenceDate})
                        - CASE
                            WHEN DATEFROMPARTS(YEAR(#{referenceDate}), MONTH(v.birthday), DAY(v.birthday)) &gt; #{referenceDate} THEN 1
                            ELSE 0
                          END
                    WHEN v.age IS NOT NULL THEN CAST(v.age AS INT)
                    ELSE NULL
                END AS ageValue,
                sh7_saleplu.sale_no AS saleNo,
                CAST(sh7_saleplu.act_money AS DECIMAL(20, 6)) AS actMoney,
                CAST(sh7_saleplu.sale_quantity AS DECIMAL(20, 6)) AS saleQuantity
            FROM sh7_saleplu
            LEFT JOIN vip_base_info v
              ON v.card_no = sh7_saleplu.card_no
            WHERE CAST(sh7_saleplu.xsdate AS DATE) &gt;= #{param.periodStart}
              AND CAST(sh7_saleplu.xsdate AS DATE) &lt;= #{param.periodEnd}
              <include refid="classFilterSh7"/>
              <include refid="storeFilterSh7"/>
        )
        SELECT
            gender,
            ageValue,
            ISNULL(SUM(actMoney), 0) AS totalSales,
            ISNULL(SUM(saleQuantity), 0) AS totalSaleQuantity,
            ISNULL(CAST(COUNT(DISTINCT saleNo) AS DECIMAL(20, 2)), 0) AS totalCustomerCount
        FROM base_data
        GROUP BY gender, ageValue
        ORDER BY gender ASC, ageValue ASC
    </select>

    <select id="aggregateAbcProductMetrics" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceAbcProductAggRow">
        SELECT
            product_no AS productNo,
            MAX(product_barcode) AS productBarcode,
            MAX(product_name) AS productName,
            COUNT(DISTINCT CASE WHEN sales &gt; 0 THEN store_no ELSE NULL END) AS storeNum,
            ISNULL(SUM(CAST(sale_quantity AS DECIMAL(20, 6))), 0) AS saleQuantity,
            ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS sales,
            ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS gross,
            ISNULL(SUM(CAST(sale_cost AS DECIMAL(20, 6))), 0) AS saleCost,
            CASE WHEN MAX(CASE WHEN ISNULL(LTRIM(RTRIM(cx_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN 1 ELSE 0 END) = 1
                 THEN '1' ELSE '0' END AS promotionFlag,
            MIN(sale_date) AS firstSaleDate
        FROM fact_sales_day
        WHERE sale_date &gt;= #{param.periodStart}
          AND sale_date &lt;= #{param.periodEnd}
          <include refid="classFilterSales"/>
          <include refid="storeFilterSales"/>
        GROUP BY product_no
    </select>

    <select id="aggregateAbcProductStock" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceAbcProductStockRow">
        SELECT
            product_no AS productNo,
            ISNULL(SUM(CAST(stock_quantity AS DECIMAL(20, 6))), 0) AS stockQuantity,
            ISNULL(SUM(CAST(stock_sale_money AS DECIMAL(20, 6))), 0) AS stockSaleMoney
        FROM fact_stock_day
        WHERE stock_date = #{stockDate}
          <include refid="classFilterStock"/>
          <include refid="storeFilterStock"/>
        GROUP BY product_no
    </select>

    <select id="selectAbcProductMeta" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceAbcProductMetaRow">
        SELECT
            p.product_no AS productNo,
            MAX(p.product_barcode) AS productBarcode,
            MAX(p.product_name) AS productName,
            MAX(p.status_name) AS productStatus,
            CAST(MAX(CAST(ISNULL(p.status_no, 0) AS INT)) AS NVARCHAR(20)) AS productStatusNo,
            MAX(p.product_brand) AS brandName,
            MAX(p.product_spec) AS spec,
            ISNULL(MAX(CAST(p.product_taxin_inprice AS DECIMAL(20, 6))), 0) AS inPrice,
            ISNULL(MAX(CAST(p.product_saleprice AS DECIMAL(20, 6))), 0) AS salesPrice,
            MAX(p.seasonable_flag) AS seasonableFlag,
            CASE WHEN MAX(CASE WHEN ISNULL(LTRIM(RTRIM(p.seasonable_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN 1 ELSE 0 END) = 1
                THEN N'是' ELSE N'否' END AS seasonableFlagName,
            MAX(CAST(p.seasonable_start_date AS DATE)) AS seasonableStartDate,
            MAX(CAST(p.seasonable_end_date AS DATE)) AS seasonableEndDate,
            MAX(CAST(p.first_order_date AS DATE)) AS firstSaleDate,
            <choose>
                <when test="param.classLevel == 1">
                    MAX(p.one_class_no) AS classNo,
                    MAX(p.one_class_name) AS className,
                    1 AS classLevel
                </when>
                <when test="param.classLevel == 2">
                    MAX(p.two_class_no) AS classNo,
                    MAX(p.two_class_name) AS className,
                    2 AS classLevel
                </when>
                <when test="param.classLevel == 3">
                    MAX(p.three_class_no) AS classNo,
                    MAX(p.three_class_name) AS className,
                    3 AS classLevel
                </when>
                <when test="param.classLevel == 4">
                    MAX(p.four_class_no) AS classNo,
                    MAX(p.four_class_name) AS className,
                    4 AS classLevel
                </when>
                <otherwise>
                    MAX(p.one_class_no) AS classNo,
                    MAX(p.one_class_name) AS className,
                    1 AS classLevel
                </otherwise>
            </choose>
        FROM base_dept_products p
        WHERE 1 = 1
          <include refid="classFilterDeptProducts"/>
          <include refid="storeFilterDeptProducts"/>
        GROUP BY p.product_no
    </select>

    <select id="aggregateBrandMetrics" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceBrandAggRow">
        WITH brand_dim AS (
            SELECT
                p.product_no AS productNo,
                MAX(NULLIF(LTRIM(RTRIM(p.product_brand)), '')) AS productBrand
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY p.product_no
        ),
        sales_base AS (
            SELECT
                ISNULL(NULLIF(LTRIM(RTRIM(d.productBrand)), ''), N'/') AS brandNo,
                MAX(NULLIF(LTRIM(RTRIM(d.productBrand)), '')) AS productBrand,
                COUNT(DISTINCT CASE WHEN sales &gt; 0 THEN fact_sales_day.product_no ELSE NULL END) AS skuCount,
                COUNT(DISTINCT CASE WHEN sales &gt; 0 THEN fact_sales_day.store_no ELSE NULL END) AS activeStoreCount,
                ISNULL(SUM(CAST(sale_quantity AS DECIMAL(20, 6))), 0) AS saleQuantity,
                ISNULL(SUM(CAST(sales AS DECIMAL(20, 6))), 0) AS sales,
                ISNULL(SUM(CAST(gross AS DECIMAL(20, 6))), 0) AS gross,
                ISNULL(SUM(CAST(sale_cost AS DECIMAL(20, 6))), 0) AS saleCost,
                COUNT(DISTINCT CASE WHEN ISNULL(LTRIM(RTRIM(cx_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN fact_sales_day.product_no ELSE NULL END) AS activitySku
            FROM fact_sales_day
            LEFT JOIN brand_dim d
              ON d.productNo = fact_sales_day.product_no
            WHERE sale_date &gt;= #{param.periodStart}
              AND sale_date &lt;= #{param.periodEnd}
              <include refid="classFilterSales"/>
              <include refid="storeFilterSales"/>
            GROUP BY ISNULL(NULLIF(LTRIM(RTRIM(d.productBrand)), ''), N'/')
        ),
        brand_meta AS (
            SELECT
                ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') AS brandNo,
                CASE
                    WHEN MAX(CASE WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') = N'/' THEN 1 ELSE 0 END) = 1 THEN N'/'
                    ELSE N'1'
                END AS brandType,
                CASE
                    WHEN MAX(CASE WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') = N'/' THEN 1 ELSE 0 END) = 1 THEN N'无品牌'
                    ELSE N'其他品牌'
                END AS brandTypeName
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/')
        )
        SELECT
            s.brandNo,
            ISNULL(s.productBrand, CASE WHEN s.brandNo = N'/' THEN N'无品牌' ELSE s.brandNo END) AS productBrand,
            ISNULL(m.brandType, CASE WHEN s.brandNo = N'/' THEN N'/' ELSE N'1' END) AS brandType,
            ISNULL(m.brandTypeName, CASE WHEN s.brandNo = N'/' THEN N'无品牌' ELSE N'其他品牌' END) AS brandTypeName,
            CAST(ISNULL(s.skuCount, 0) AS INT) AS skuCount,
            CAST(ISNULL(s.activeStoreCount, 0) AS INT) AS activeStoreCount,
            ISNULL(s.saleQuantity, 0) AS saleQuantity,
            ISNULL(s.sales, 0) AS sales,
            ISNULL(s.gross, 0) AS gross,
            ISNULL(s.saleCost, 0) AS saleCost,
            CAST(ISNULL(s.activitySku, 0) AS INT) AS activitySku
        FROM sales_base s
        LEFT JOIN brand_meta m
          ON m.brandNo = s.brandNo
        ORDER BY s.sales DESC, s.brandNo ASC
    </select>

    <select id="aggregateBrandStock" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceBrandStockRow">
        WITH brand_dim AS (
            SELECT
                p.product_no AS productNo,
                MAX(NULLIF(LTRIM(RTRIM(p.product_brand)), '')) AS productBrand
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY p.product_no
        ),
        daily_stock AS (
            SELECT
                stock_date,
                ISNULL(NULLIF(LTRIM(RTRIM(d.productBrand)), ''), N'/') AS brandNo,
                ISNULL(SUM(CAST(stock_quantity AS DECIMAL(20, 6))), 0) AS dayStockQuantity
            FROM fact_stock_day
            LEFT JOIN brand_dim d
              ON d.productNo = fact_stock_day.product_no
            WHERE stock_date &gt;= #{param.periodStart}
              AND stock_date &lt;= #{param.periodEnd}
              <include refid="classFilterStock"/>
              <include refid="storeFilterStock"/>
            GROUP BY stock_date, ISNULL(NULLIF(LTRIM(RTRIM(d.productBrand)), ''), N'/')
        )
        SELECT
            brandNo,
            ISNULL(AVG(CAST(dayStockQuantity AS DECIMAL(20, 6))), 0) AS avgStockQuantity
        FROM daily_stock
        GROUP BY brandNo
    </select>

    <select id="selectBrandMeta" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceBrandMetaRow">
        SELECT
            ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') AS brandNo,
            MAX(NULLIF(LTRIM(RTRIM(p.product_brand)), '')) AS productBrand,
            CASE
                WHEN MAX(CASE WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') = N'/' THEN 1 ELSE 0 END) = 1 THEN N'/'
                ELSE N'1'
            END AS brandType,
            CASE
                WHEN MAX(CASE WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/') = N'/' THEN 1 ELSE 0 END) = 1 THEN N'无品牌'
                ELSE N'其他品牌'
            END AS brandTypeName,
            CAST(NULL AS NVARCHAR(128)) AS ownBrandNo,
            CAST(NULL AS NVARCHAR(255)) AS ownBrandName,
            MIN(CAST(p.first_order_date AS DATE)) AS firstSaleDate
        FROM base_dept_products p
        WHERE 1 = 1
          <include refid="classFilterDeptProducts"/>
          <include refid="storeFilterDeptProducts"/>
        GROUP BY ISNULL(NULLIF(LTRIM(RTRIM(p.product_brand)), ''), N'/')
    </select>

    <select id="aggregateSpecMetrics" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceSpecAggRow">
        WITH spec_meta AS (
            SELECT
                p.product_no AS productNo,
                ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') AS specNo,
                ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') AS specName,
                CASE
                    WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') = N'/' THEN N'/'
                    ELSE N'1'
                END AS specType,
                CASE
                    WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') = N'/' THEN N'无规格'
                    ELSE N'其他规格'
                END AS specTypeName
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY p.product_no, ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/')
        ),
        sales_base AS (
            SELECT
                m.specNo AS specNo,
                MAX(m.specName) AS specName,
                MAX(m.specType) AS specType,
                MAX(m.specTypeName) AS specTypeName,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.product_no ELSE NULL END) AS skuCount,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.store_no ELSE NULL END) AS activeStoreCount,
                ISNULL(SUM(CAST(s.sale_quantity AS DECIMAL(20, 6))), 0) AS saleQuantity,
                ISNULL(SUM(CAST(s.sales AS DECIMAL(20, 6))), 0) AS sales,
                ISNULL(SUM(CAST(s.gross AS DECIMAL(20, 6))), 0) AS gross,
                ISNULL(SUM(CAST(s.sale_cost AS DECIMAL(20, 6))), 0) AS saleCost,
                COUNT(DISTINCT CASE WHEN ISNULL(LTRIM(RTRIM(s.cx_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN s.product_no ELSE NULL END) AS activitySku
            FROM fact_sales_day s
            INNER JOIN spec_meta m
              ON m.productNo = s.product_no
            WHERE s.sale_date &gt;= #{param.periodStart}
              AND s.sale_date &lt;= #{param.periodEnd}
              <include refid="classFilterSales"/>
              <include refid="storeFilterSales"/>
            GROUP BY m.specNo
        )
        SELECT
            specNo,
            specName,
            specType,
            specTypeName,
            CAST(ISNULL(skuCount, 0) AS INT) AS skuCount,
            CAST(ISNULL(activeStoreCount, 0) AS INT) AS activeStoreCount,
            ISNULL(saleQuantity, 0) AS saleQuantity,
            ISNULL(sales, 0) AS sales,
            ISNULL(gross, 0) AS gross,
            ISNULL(saleCost, 0) AS saleCost,
            CAST(ISNULL(activitySku, 0) AS INT) AS activitySku
        FROM sales_base
        ORDER BY sales DESC, specNo ASC
    </select>

    <select id="aggregateSpecStock" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceSpecStockRow">
        WITH spec_meta AS (
            SELECT
                p.product_no AS productNo,
                ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') AS specNo
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY p.product_no, ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/')
        ),
        daily_stock AS (
            SELECT
                s.stock_date AS stockDate,
                m.specNo AS specNo,
                ISNULL(SUM(CAST(s.stock_quantity AS DECIMAL(20, 6))), 0) AS dayStockQuantity
            FROM fact_stock_day s
            INNER JOIN spec_meta m
              ON m.productNo = s.product_no
            WHERE s.stock_date &gt;= #{param.periodStart}
              AND s.stock_date &lt;= #{param.periodEnd}
              <include refid="classFilterStock"/>
              <include refid="storeFilterStock"/>
            GROUP BY s.stock_date, m.specNo
        )
        SELECT
            specNo,
            ISNULL(AVG(CAST(dayStockQuantity AS DECIMAL(20, 6))), 0) AS avgStockQuantity
        FROM daily_stock
        GROUP BY specNo
    </select>

    <select id="selectSpecMeta" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceSpecMetaRow">
        SELECT
            ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') AS specNo,
            ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') AS specName,
            CASE
                WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') = N'/' THEN N'/'
                ELSE N'1'
            END AS specType,
            CASE
                WHEN ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/') = N'/' THEN N'无规格'
                ELSE N'其他规格'
            END AS specTypeName,
            MIN(CAST(p.first_order_date AS DATE)) AS firstSaleDate
        FROM base_dept_products p
        WHERE 1 = 1
          <include refid="classFilterDeptProducts"/>
          <include refid="storeFilterDeptProducts"/>
        GROUP BY ISNULL(NULLIF(LTRIM(RTRIM(p.product_spec)), ''), N'/')
    </select>

    <select id="aggregateTagMetrics" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceTagAggRow">
        WITH product_scope AS (
            SELECT DISTINCT p.product_no AS productNo
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
        ),
        tag_scope AS (
            SELECT
                t.product_no AS productNo,
                t.tag_type_no AS tagType,
                MAX(t.tag_type_name) AS tagTypeName,
                t.tag_no AS tagNo,
                MAX(t.tag_name) AS tagName,
                CONCAT(t.tag_type_no, N'|', t.tag_no) AS metricKey,
                CAST(0 AS BIT) AS isUntagged
            FROM product_tag_info t
            INNER JOIN product_scope p
              ON p.productNo = t.product_no
            GROUP BY t.product_no, t.tag_type_no, t.tag_no
        ),
        tag_sales AS (
            SELECT
                ts.tagType,
                MAX(ts.tagTypeName) AS tagTypeName,
                ts.tagNo,
                MAX(ts.tagName) AS tagName,
                ts.metricKey,
                CAST(0 AS BIT) AS isUntagged,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.product_no ELSE NULL END) AS skuCount,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.store_no ELSE NULL END) AS activeStoreCount,
                ISNULL(SUM(CAST(s.sale_quantity AS DECIMAL(20, 6))), 0) AS saleQuantity,
                ISNULL(SUM(CAST(s.sales AS DECIMAL(20, 6))), 0) AS sales,
                ISNULL(SUM(CAST(s.gross AS DECIMAL(20, 6))), 0) AS gross,
                ISNULL(SUM(CAST(s.sale_cost AS DECIMAL(20, 6))), 0) AS saleCost,
                COUNT(DISTINCT CASE WHEN ISNULL(LTRIM(RTRIM(s.cx_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN s.product_no ELSE NULL END) AS activitySku
            FROM fact_sales_day s
            INNER JOIN tag_scope ts
              ON ts.productNo = s.product_no
            WHERE s.sale_date &gt;= #{param.periodStart}
              AND s.sale_date &lt;= #{param.periodEnd}
              <include refid="classFilterSales"/>
              <include refid="storeFilterSales"/>
            GROUP BY ts.tagType, ts.tagNo, ts.metricKey
        ),
        untagged_sales AS (
            SELECT
                N'*' AS tagType,
                N'无标签' AS tagTypeName,
                CAST(NULL AS NVARCHAR(128)) AS tagNo,
                N'无标签' AS tagName,
                N'__UNTAGGED__' AS metricKey,
                CAST(1 AS BIT) AS isUntagged,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.product_no ELSE NULL END) AS skuCount,
                COUNT(DISTINCT CASE WHEN s.sales &gt; 0 THEN s.store_no ELSE NULL END) AS activeStoreCount,
                ISNULL(SUM(CAST(s.sale_quantity AS DECIMAL(20, 6))), 0) AS saleQuantity,
                ISNULL(SUM(CAST(s.sales AS DECIMAL(20, 6))), 0) AS sales,
                ISNULL(SUM(CAST(s.gross AS DECIMAL(20, 6))), 0) AS gross,
                ISNULL(SUM(CAST(s.sale_cost AS DECIMAL(20, 6))), 0) AS saleCost,
                COUNT(DISTINCT CASE WHEN ISNULL(LTRIM(RTRIM(s.cx_flag)), '0') IN ('1', 'Y', 'y', 'true', 'TRUE') THEN s.product_no ELSE NULL END) AS activitySku
            FROM fact_sales_day s
            INNER JOIN product_scope p
              ON p.productNo = s.product_no
            WHERE s.sale_date &gt;= #{param.periodStart}
              AND s.sale_date &lt;= #{param.periodEnd}
              <include refid="classFilterSales"/>
              <include refid="storeFilterSales"/>
              AND NOT EXISTS (
                  SELECT 1
                  FROM product_tag_info t
                  WHERE t.product_no = s.product_no
              )
        )
        SELECT
            tagType,
            tagTypeName,
            tagNo,
            tagName,
            metricKey,
            isUntagged,
            CAST(ISNULL(skuCount, 0) AS INT) AS skuCount,
            CAST(ISNULL(activeStoreCount, 0) AS INT) AS activeStoreCount,
            ISNULL(saleQuantity, 0) AS saleQuantity,
            ISNULL(sales, 0) AS sales,
            ISNULL(gross, 0) AS gross,
            ISNULL(saleCost, 0) AS saleCost,
            CAST(ISNULL(activitySku, 0) AS INT) AS activitySku
        FROM (
            SELECT * FROM tag_sales
            UNION ALL
            SELECT * FROM untagged_sales
        ) x
        ORDER BY sales DESC, metricKey ASC
    </select>

    <select id="aggregateTagStock" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceTagStockRow">
        WITH product_scope AS (
            SELECT DISTINCT p.product_no AS productNo
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
        ),
        tag_scope AS (
            SELECT
                t.product_no AS productNo,
                CONCAT(t.tag_type_no, N'|', t.tag_no) AS metricKey
            FROM product_tag_info t
            INNER JOIN product_scope p
              ON p.productNo = t.product_no
            GROUP BY t.product_no, t.tag_type_no, t.tag_no
        ),
        tag_daily_stock AS (
            SELECT
                s.stock_date AS stockDate,
                ts.metricKey AS metricKey,
                ISNULL(SUM(CAST(s.stock_quantity AS DECIMAL(20, 6))), 0) AS dayStockQuantity
            FROM fact_stock_day s
            INNER JOIN tag_scope ts
              ON ts.productNo = s.product_no
            WHERE s.stock_date &gt;= #{param.periodStart}
              AND s.stock_date &lt;= #{param.periodEnd}
              <include refid="classFilterStock"/>
              <include refid="storeFilterStock"/>
            GROUP BY s.stock_date, ts.metricKey
        ),
        untagged_daily_stock AS (
            SELECT
                s.stock_date AS stockDate,
                N'__UNTAGGED__' AS metricKey,
                ISNULL(SUM(CAST(s.stock_quantity AS DECIMAL(20, 6))), 0) AS dayStockQuantity
            FROM fact_stock_day s
            INNER JOIN product_scope p
              ON p.productNo = s.product_no
            WHERE s.stock_date &gt;= #{param.periodStart}
              AND s.stock_date &lt;= #{param.periodEnd}
              <include refid="classFilterStock"/>
              <include refid="storeFilterStock"/>
              AND NOT EXISTS (
                  SELECT 1
                  FROM product_tag_info t
                  WHERE t.product_no = s.product_no
              )
            GROUP BY s.stock_date
        )
        SELECT
            metricKey,
            ISNULL(AVG(CAST(dayStockQuantity AS DECIMAL(20, 6))), 0) AS avgStockQuantity
        FROM (
            SELECT * FROM tag_daily_stock
            UNION ALL
            SELECT * FROM untagged_daily_stock
        ) x
        GROUP BY metricKey
    </select>

    <select id="selectTagMeta" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisSourceTagMetaRow">
        WITH product_scope AS (
            SELECT
                p.product_no AS productNo,
                MIN(CAST(p.first_order_date AS DATE)) AS firstSaleDate
            FROM base_dept_products p
            WHERE 1 = 1
              <include refid="classFilterDeptProducts"/>
              <include refid="storeFilterDeptProducts"/>
            GROUP BY p.product_no
        ),
        tag_meta AS (
            SELECT
                t.tag_type_no AS tagType,
                MAX(t.tag_type_name) AS tagTypeName,
                t.tag_no AS tagNo,
                MAX(t.tag_name) AS tagName,
                CONCAT(t.tag_type_no, N'|', t.tag_no) AS metricKey,
                CAST(0 AS BIT) AS isUntagged,
                MIN(p.firstSaleDate) AS firstSaleDate
            FROM product_tag_info t
            INNER JOIN product_scope p
              ON p.productNo = t.product_no
            GROUP BY t.tag_type_no, t.tag_no
        ),
        untagged_meta AS (
            SELECT
                N'*' AS tagType,
                N'无标签' AS tagTypeName,
                CAST(NULL AS NVARCHAR(128)) AS tagNo,
                N'无标签' AS tagName,
                N'__UNTAGGED__' AS metricKey,
                CAST(1 AS BIT) AS isUntagged,
                MIN(p.firstSaleDate) AS firstSaleDate
            FROM product_scope p
            WHERE NOT EXISTS (
                SELECT 1
                FROM product_tag_info t
                WHERE t.product_no = p.productNo
            )
        )
        SELECT
            tagType,
            tagTypeName,
            tagNo,
            tagName,
            metricKey,
            isUntagged,
            firstSaleDate
        FROM (
            SELECT * FROM tag_meta
            UNION ALL
            SELECT * FROM untagged_meta
        ) x
    </select>

    <select id="selectOnlineChannelDictRows" resultType="org.dromara.diagnosis.infrastructure.model.DiagnosisDictRow">
        SELECT
            d.dict_code AS dictCode,
            t.dict_id AS dictId,
            d.dict_sort AS dictSort,
            d.dict_label AS dictLabel,
            d.dict_value AS dictValue,
            CONVERT(VARCHAR(19), d.create_time, 120) AS createTime
        FROM sys_dict_data d
                 LEFT JOIN sys_dict_type t
                           ON t.dict_type = d.dict_type
                               AND t.tenant_id = d.tenant_id
        WHERE d.tenant_id = N'000000'
          AND d.dict_type IN
        <foreach collection="dictTypes" item="dictType" open="(" close=")" separator=",">
            #{dictType}
        </foreach>
        ORDER BY d.dict_type ASC, d.dict_sort ASC, d.dict_code ASC
    </select>

</mapper>
