# 品类诊断系统数据与字段清单

## 1. 设计目标

这份文档用于说明：如果要把当前这套品类诊断系统完整落地，需要准备哪些**原始数据、配置数据、结果数据和接口字段**。

适用范围：

- 品类诊断
- 品牌分析
- 规格分析
- 标签分析
- ABC 结构
- 价格带分析
- 供应商分析
- 客户分析
- 渠道表现
- 子类贡献
- 全类检测
- 预计算任务中心

数据来源主要参考：

- `Datainterface_YY.sql`
- `script/tmp/*.sql`
- `category-analysis-modules/category-analysis-diagnosis/**`

---

## 2. 总体数据分层

### 2.1 原始主数据

用于描述“是谁、属于什么、归到哪里”：

- 门店 / 组织
- 品类树
- 商品主数据
- 品牌
- 供应商
- 会员
- 标签
- 字典

### 2.2 原始事实数据

用于描述“发生了什么”：

- 日销售
- 日库存
- 销售流水
- 损耗
- 供应商成本 / 订单

### 2.3 结果与任务数据

用于描述“系统算出了什么、算到哪一步了”：

- 预计算任务
- 预计算窗口
- 预计算事件
- 结果发布版本
- 各业务模块结果表

### 2.4 接口输入数据

用于描述“用户点了什么筛选条件”：

- sessionId
- 门店范围
- 品类范围
- 时间窗口
- 对比窗口
- ABC / 价格带 / 标签 / 品牌 / 规格 / 状态筛选

---

## 3. 必备原始数据清单

### 3.1 门店 / 组织数据

#### `base_department`

门店维表，至少需要这些字段：

- `store_no`：门店编码
- `store_name`：门店名称
- `store_format_no`：业态编码
- `store_format_name`：业态名称
- `business_circle_no`：商圈编码
- `business_circle_name`：商圈名称
- `store_group_no`：门店组编码
- `store_group_name`：门店组名称
- `store_type_no`：门店类型编码
- `store_type_name`：门店类型名称
- `preorgcode`：前置组织编码
- `preorgname`：前置组织名称
- `status_no`：状态编码
- `status_name`：状态名称
- `store_staff_num`：门店员工数
- `store_area`：门店面积
- `longitude`：经度
- `latitude`：纬度
- `start_date`：启用日期
- `end_date`：停用日期

用途：

- 门店筛选
- 门店归属分析
- 业态 / 商圈 / 分组筛选

#### `base_class_sku`

门店-品类-角色映射表，至少需要：

- `store_no`：门店编码
- `store_name`：门店名称
- `class_no`：品类编码
- `class_name`：品类名称
- `class_sku`：品类 SKU 数
- `class_role`：品类角色

用途：

- 品类角色分析
- 全类检测
- 类目树筛选

#### `base_division`

部门层级映射表，至少需要：

- `div_no`：一级部门编码
- `div_name`：一级部门名称
- `subdiv_no`：二级部门编码
- `subdiv_name`：二级部门名称
- `bclass_no`：三级部门编码
- `bclass_name`：三级部门名称
- `mclass_no`：四级部门编码
- `mclass_name`：四级部门名称
- `sclass_no`：五级部门编码
- `sclass_name`：五级部门名称

用途：

- 部门层级映射
- 品类归属扩展

---

### 3.2 品类树数据

#### `base_class`

完整品类树，至少需要：

- `one_class_no`：一级品类编码
- `one_class_name`：一级品类名称
- `two_class_no`：二级品类编码
- `two_class_name`：二级品类名称
- `three_class_no`：三级品类编码
- `three_class_name`：三级品类名称
- `four_class_no`：四级品类编码
- `four_class_name`：四级品类名称
- `five_class_no`：五级品类编码
- `five_class_name`：五级品类名称

用途：

- 品类树展示
- 品类层级转换
- 会话查询中的 classLevel/classNo 定位

#### `base_dept_products`

门店-品类-商品联表，是很多分析的核心桥接表，至少需要：

- `store_no`：门店编码
- `store_name`：门店名称
- `one_class_no`：一级品类编码
- `one_class_name`：一级品类名称
- `two_class_no`：二级品类编码
- `two_class_name`：二级品类名称
- `three_class_no`：三级品类编码
- `three_class_name`：三级品类名称
- `four_class_no`：四级品类编码
- `four_class_name`：四级品类名称
- `five_class_no`：五级品类编码
- `five_class_name`：五级品类名称
- `div_no`：一级部门编码
- `div_name`：一级部门名称
- `subdiv_no`：二级部门编码
- `subdiv_name`：二级部门名称
- `bclass_no`：三级部门编码
- `bclass_name`：三级部门名称
- `mclass_no`：四级部门编码
- `mclass_name`：四级部门名称
- `sclass_no`：五级部门编码
- `sclass_name`：五级部门名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `brand_no`：品牌编码
- `product_brand`：商品品牌名称
- `product_spec`：商品规格
- `expireddate`：保质期/过期天数
- `expiredun`：保质期单位
- `pspack_unit`：包装单位
- `product_unit`：基础单位
- `product_taxin_inprice`：含税进价
- `product_saleprice`：商品零售价
- `status_no`：状态编码
- `status_name`：状态名称
- `sale_type_no`：售卖类型编码
- `sale_type`：售卖类型名称
- `logistic_type`：物流类型
- `seasonable_flag`：是否季节品标志
- `seasonable_start_date`：季节品开始日期
- `seasonable_end_date`：季节品结束日期
- `seasonable_info`：季节品说明
- `display_position`：陈列位置
- `sequence_num`：陈列顺序
- `face_num`：陈列面数
- `depth_num`：陈列深度
- `full_display_quantity`：满陈列数量
- `first_order_date`：首次订货日期

用途：

- 品类树下钻
- 商品状态筛选
- 品牌分析
- 规格分析
- 全类检测
- 门店-品类可售关系

---

### 3.3 商品主数据

#### `base_product`

商品主数据表，至少需要：

- `one_class_no` ~ `five_class_name`：一级到五级品类编码/名称
- `div_no` ~ `sclass_name`：一级到五级部门编码/名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `brand_no`：品牌编码
- `product_brand`：商品品牌名称
- `product_spec`：商品规格
- `expireddate`：保质期/过期天数
- `expiredun`：保质期单位
- `pspack_unit`：包装单位
- `product_unit`：基础单位
- `product_taxin_inprice`：含税进价
- `product_saleprice`：商品零售价
- `status_no`：状态编码
- `status_name`：状态名称
- `sale_type_no`：售卖类型编码
- `sale_type`：售卖类型名称
- `logistic_type`：物流类型
- `seasonable_flag`：是否季节品标志
- `seasonable_start_date`：季节品开始日期
- `seasonable_end_date`：季节品结束日期
- `seasonable_info`：季节品说明
- `width`：商品宽度
- `height`：商品高度
- `depth`：商品深度
- `color`：商品颜色
- `first_order_date`：首次订货日期

用途：

- 商品维度展示
- 品牌 / 规格 / ABC / 滞销 / 价格带分析

---

### 3.4 品牌数据

#### `base_product` / `base_dept_products`

品牌基础字段：

- `brand_no`：品牌编码
- `product_brand`：商品品牌名称

#### 待确认字段

- `brandType`：品牌类型
- `brandTypeName`：品牌类型名称

当前仓库里尚未找到稳定、明确的品牌类型源，已知需要重点确认：

- `product_tag_info` 是否可以映射品牌类型
- 是否存在独立品牌类型维表
- `brand_no` 到 `brandType` 的业务规则

---

### 3.5 供应商数据

#### `base_vendor_info`

供应商主数据，至少需要：

- `vendor_no`：供应商编码
- `vendor_name`：供应商名称
- `vendor_info`：供应商级别
- `status_no`：状态编码
- `status_name`：状态名称

#### `base_vendor_product_info`

商品-供应商映射，至少需要：

- `product_no`：商品编码
- `product_barcode`：商品条码
- `vendor_no`：供应商编码
- `vendor_name`：供应商名称
- `status_no`：状态编码
- `status_name`：状态名称

#### `base_vendor_contract_info`

供应商合同信息，至少需要：

- `vendor_no`：供应商编码
- `contract_no`：合同编码
- `contract_name`：合同名称
- `contract_start_date`：合同开始时间
- `contract_end_date`：合同结束时间
- `account_days`：账期
- `status_no`：状态编码
- `status_name`：状态名称

#### `product_vendor_cost_info`

供应商费用信息，至少需要：

- `vendor_no`：供应商编码
- `final_store_no`：收取对象编码
- `final_store_name`：收取对象名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `cost_no`：费用编号
- `cost_name`：费用名称
- `cost_money`：费用金额
- `cost_date`：费用发生日期

用途：

- 供应商分析
- 订单 / 费用 / 经营表现归因

---

### 3.6 会员 / 客户数据

#### `vip_base_info`

会员基础信息，至少需要：

- `card_no`：会员卡号
- `card_type`：会员卡类型
- `gender`：性别
- `age` 或 `birthday`：年龄或出生日期
- `store_no`：门店编码
- `create_time`：创建时间

用途：

- 客户分析
- 年龄段分析
- 性别分析

#### `history_sh7_saleplu`

会员销售流水，至少需要：

- `store_no`：门店编码
- `sale_channel`：销售渠道类型
- `online_type`：线上渠道类型
- `online_name`：线上渠道名称
- `card_no`：会员卡号
- `xsdate`：销售日期时间
- `sale_no`：销售流水号
- `product_no`：商品编码
- `product_barcode`：商品条码
- `sale_quantity`：销售量
- `nomal_price`：正常售价
- `price`：实际售价
- `vip_price`：会员价
- `taxin_money`：应收金额
- `act_money`：实收金额
- `settlement_date`：结算日期

用途：

- 客户分析
- 客单价
- 流水识别
- 会员消费行为分析

---

### 3.7 标签数据

#### `product_tag_info`

商品标签表，至少需要：

- `product_no`：商品编码
- `product_barcode`：商品条码
- `tag_type_no`：标签类型编码
- `tag_type_name`：标签类型名称
- `tag_no`：标签编码
- `tag_name`：标签名称

用途：

- 标签分析
- 商品分群
- 滞销 / 新品 / 特征标签

#### 字典依赖

- `class_role_type`：品类角色字典类型

用途：

- 品类角色字典
- 品类树筛选项

---

### 3.8 销售事实数据

#### `fact_sales_day`

日销售事实表，核心字段：

- `store_no`：门店编码
- `store_name`：门店名称
- `sale_channel`：销售渠道类型
- `online_type`：线上渠道类型
- `online_name`：线上渠道名称
- `one_class_no` ~ `five_class_name`：一级到五级品类编码/名称
- `div_no` ~ `sclass_name`：一级到五级部门编码/名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `now_product_saleprice`：当日售价
- `sale_quantity`：销售量
- `sales`：销售额
- `sale_cost`：销售成本
- `gross`：毛利额
- `customer_return_quantity`：顾客退货数量
- `customer_return`：顾客退货金额
- `cx_flag`：是否参与促销标志
- `sale_date`：发生日期

#### `history_fact_sales_day`

历史日销售表，字段与 `fact_sales_day` 基本一致，作为历史归档或兼容源。

用途：

- 品类诊断
- 品牌分析
- 规格分析
- 标签分析
- 渠道分析
- 价格带分析
- 全类检测

---

### 3.9 库存事实数据

#### `fact_stock_day`

日库存事实表，核心字段：

- `store_no`：门店编码
- `store_name`：门店名称
- `one_class_no` ~ `five_class_name`：一级到五级品类编码/名称
- `div_no` ~ `sclass_name`：一级到五级部门编码/名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `stock_quantity`：库存量
- `stock_unit`：库存单位
- `onroad_quantity`：在途数量
- `stock_cost`：库存总成本
- `stock_sale_money`：库存售价总额
- `last_order_date`：最近进货日期
- `stock_date`：发生日期

#### `history_fact_stock_day`

历史库存表，字段与 `fact_stock_day` 基本一致。

#### `fact_stock_live`

实时库存表，字段与库存日表基本一致，但 `stock_date` 为 datetime。

用途：

- 周转率
- 周转天数
- GMROI
- 库销比
- 库存贡献分析

---

### 3.10 损耗事实数据

#### `fact_loss_day`

日损耗事实表，核心字段：

- `store_no`：门店编码
- `store_name`：门店名称
- `one_class_no` ~ `five_class_name`：一级到五级品类编码/名称
- `div_no` ~ `sclass_name`：一级到五级部门编码/名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `product_loss`：损耗金额
- `product_loss_quantity`：损耗数量
- `loss_date`：发生日期
- `loss_type_no`：损耗类型编码
- `loss_type_name`：损耗类型名称

#### `history_fact_loss_day`

历史损耗表，字段与 `fact_loss_day` 基本一致。

用途：

- 损耗分析
- 经营异常分析

---

### 3.11 其他可选事实数据

#### `fact_vendor_order_month`

用于供应商订单分析，核心字段：

- `order_no`：订单编号
- `order_month`：订单月份
- `vendor_no`：供应商编码
- `final_store_no`：收取对象编码
- `final_store_name`：收取对象名称
- `product_no`：商品编码
- `product_barcode`：商品条码
- `product_name`：商品名称
- `order_date`：订购时间
- `order_quantity`：订购数量
- `arrival_date`：到货时间
- `arrival_quantity`：到货数量

用途：

- 供应商分析
- 到货 / 订货表现分析

---

## 4. 预计算与结果层

### 4.1 任务层

#### `diag_precompute_job`

任务主表，关键字段：

- `job_code`：任务编码
- `module_code`：模块编码
- `status_code`：状态编码
- `priority`：优先级
- `request_json`：请求参数 JSON
- `request_hash`：请求参数摘要
- `read_range_type`：读取范围类型
- `read_start`：读取开始日期
- `read_end`：读取结束日期
- `window_types`：窗口类型列表
- `force_rebuild`：是否强制重算
- `max_retry`：最大重试次数
- `retry_count`：已重试次数
- `progress_percent`：进度百分比
- `current_stage`：当前阶段
- `total_windows`：窗口总数
- `done_windows`：已完成窗口数
- `rows_read`：已读取行数
- `rows_written`：已写入行数
- `orchestrator_status`：编排状态
- `module_progress_json`：模块进度 JSON
- `submitted_by`：提交人
- `submitted_time`：提交时间
- `started_time`：开始时间
- `finished_time`：结束时间
- `error_message`：错误信息

#### `diag_precompute_window`

窗口表，关键字段：

- `job_id`：任务 ID
- `window_key`：窗口键
- `window_type`：窗口类型
- `period_start`：本期开始日期
- `period_end`：本期结束日期
- `compare_start`：对比期开始日期
- `compare_end`：对比期结束日期
- `status_code`：状态编码
- `progress_percent`：进度百分比
- `current_stage`：当前阶段
- `data_version`：数据版本
- `retry_count`：已重试次数

#### `diag_precompute_event`

事件表，关键字段：

- `job_id`：任务 ID
- `window_id`：窗口 ID
- `event_time`：事件时间
- `event_level`：事件级别
- `event_stage`：事件阶段
- `event_message`：事件消息
- `payload_json`：事件载荷 JSON

#### `diag_result_publish_version`

结果发布版本表，关键字段：

- `query_hash`：查询条件哈希
- `data_version`：数据版本
- `publish_status`：发布状态
- `job_id`：任务 ID
- `published_time`：发布时间
- `error_summary`：错误摘要

---

### 4.2 通用诊断结果

#### `diag_result_overview`

概览结果，关键字段：

- `class_level`：品类层级
- `class_no`：品类编码
- `class_name`：品类名称
- `retail_type_id`：业态 ID
- `dept_id`：组织/门店 ID
- `business_circle_id`：商圈 ID
- `dept_group_id`：门店组 ID
- `store_no`：门店编码
- `period_start`：本期开始日期
- `period_end`：本期结束日期
- `compare_start`：对比期开始日期
- `compare_end`：对比期结束日期
- `metric_total_sales`：销售总额
- `metric_total_profit`：毛利总额
- `metric_profit_margin`：毛利率
- `metric_total_sku`：总 SKU 数
- `metric_active_sku`：动销 SKU 数
- `metric_sales_rate`：销售占比

#### `diag_result_trends`

趋势结果，关键字段：

- `metric_code`：指标编码
- `point_date`：日期点
- `current_value`：本期值
- `compare_value`：对比期值
- `growth_rate`：增长率
- `period_label`：周期标签

#### `diag_result_role_distribution`

角色分布快照，供全类检测使用。

#### `diag_result_insights`

洞察结果，供智能提示、异常归因使用。

---

### 4.3 业务模块结果表

#### 品牌

- `diag_result_brand_overview`：品牌概览结果表
- `diag_result_brand_metric`：品牌明细结果表
- `diag_result_brand_json`：品牌 JSON 结果表

关键字段：

- `brand_no`：品牌编码
- `product_brand`：商品品牌名称
- `brand_type`：品牌类型
- `new_brand_type`：新品牌类型
- `sku_count`：SKU 数
- `sales`：销售额
- `gross`：毛利额
- `gross_rate`：毛利率
- `stock_quantity`：库存量
- `turnover_rate`：周转率
- `turnover_days`：周转天数
- `gmroi`：GMROI

#### 规格

- `diag_result_spec_overview`：规格概览结果表
- `diag_result_spec_metric`：规格明细结果表
- `diag_result_spec_json`：规格 JSON 结果表

关键字段：

- `spec_no`：规格编码
- `spec_name`：规格名称
- `spec_type`：规格类型
- `new_spec_type`：新规格类型
- `sku_count`：SKU 数
- `sales`：销售额
- `gross`：毛利额
- `gross_rate`：毛利率
- `stock_quantity`：库存量
- `turnover_rate`：周转率
- `gmroi`：GMROI

#### 标签

- `diag_result_tag_metric`：标签明细结果表
- `diag_result_tag_json`：标签 JSON 结果表

关键字段：

- `tag_type`：标签类型
- `tag_type_name`：标签类型名称
- `tag_no`：标签编码
- `tag_name`：标签名称
- `metric_key`：指标键
- `sku_count`：SKU 数
- `sales`：销售额
- `gross`：毛利额
- `gross_rate`：毛利率
- `stock_quantity`：库存量

#### 供应商

- `diag_result_vendor_metric`：供应商明细结果表
- `diag_result_vendor_json`：供应商 JSON 结果表

关键字段：

- `product_vendor_no`：供应商编码
- `product_vendor_name`：供应商名称
- `product_vendor_no_name`：供应商编码+名称
- `product_vendor_status_no`：供应商状态编码
- `sku_count`：SKU 数
- `sales`：销售额
- `gross`：毛利额
- `oi_amount`：OI 金额
- `all_gross`：全部毛利
- `net_order_amount`：净订货金额

#### 渠道

- `diag_result_channel_contribution`：渠道贡献结果表
- `diag_result_channel_trend`：渠道趋势结果表

关键字段：

- `sale_channel`：销售渠道类型
- `online_type`：线上渠道类型
- `online_name`：线上渠道名称
- `channel_name`：渠道名称
- `current_sales`：本期销售额
- `current_gross`：本期毛利额
- `current_customer_count`：本期客数
- `current_customer_price`：本期客单价

#### ABC

- `diag_abc_param_config`：ABC 参数配置表
- `diag_result_abc_params`：ABC 参数结果表
- `diag_result_abc_bucket`：ABC 分桶结果表
- `diag_result_abc_matrix`：ABC 矩阵结果表
- `diag_result_abc_sku`：ABC 单品结果表

关键字段：

- `abcType`：ABC 类型
- `salesPer`：销售额占比
- `grossPer`：毛利额占比
- `salesQuantityPer`：销量占比
- `crate`：C 类占比阈值
- `arate`：A 类占比阈值
- `brate`：B 类占比阈值
- `askuRate`：A 类 SKU 占比
- `bskuRate`：B 类 SKU 占比
- `cskuRate`：C 类 SKU 占比

#### 价格带

- `diag_price_band_config`：价格带配置表
- `diag_result_price_band_range`：价格带区间结果表
- `diag_result_price_band_line`：价格带曲线结果表
- `diag_result_price_band_point`：价格带散点结果表
- `diag_result_price_band_sku`：价格带单品结果表

关键字段：

- `price_band_min`：价格带下限
- `price_band_max`：价格带上限
- `price_band_label`：价格带标签
- `sku`：SKU 数
- `sales`：销售额
- `sale_quantity`：销售量
- `gross`：毛利额
- `gross_rate`：毛利率
- `gmroi`：GMROI

#### 子类贡献

- `diag_result_subclass_contribution`：子类贡献结果表
- `diag_result_subclass_trend`：子类趋势结果表

关键字段：

- `parent_class_level`：父级品类层级
- `parent_class_no`：父级品类编码
- `sub_class_level`：子类层级
- `sub_class_no`：子类编码
- `sub_class_name`：子类名称
- `current_sales`：本期销售额
- `current_gross`：本期毛利额
- `current_sale_quantity`：本期销售量
- `current_customer_count`：本期客数
- `current_customer_price`：本期客单价
- `current_avg_inventory`：本期平均库存
- `current_turnover_rate`：本期周转率
- `current_turnover_days`：本期周转天数
- `current_gmroi`：本期 GMROI

#### 品类趋势

- `diag_result_category_performance_trend`：品类趋势结果表

关键字段：

- `period_flag`：周期标识
- `point_index`：点位序号
- `point_date`：日期点
- `class_level`：品类层级
- `class_no`：品类编码
- `class_name`：品类名称
- `sales`：销售额
- `sale_quantity`：销售量
- `gross`：毛利额
- `gross_rate`：毛利率
- `customer_count`：客数
- `customer_price`：客单价
- `sale_cost`：销售成本
- `stock_cost`：库存总成本
- `stock_cost_rate`：库存成本率

#### 单品销售 / 毛利 / GMROI / 滞销

- `diag_result_category_sales_sku`：品类销售单品结果表
- `diag_result_gross_sku`：毛利单品结果表
- `diag_result_gmroi_sku`：GMROI 单品结果表

关键字段：

- `product_no`：商品编码
- `product_name`：商品名称
- `product_status`：商品状态
- `product_status_no`：商品状态编码
- `store_num`：门店数
- `promotion_flag`：促销标志
- `sale_quantity`：销售量
- `sales`：销售额
- `gross`：毛利额
- `gross_rate`：毛利率
- `stock_quantity`：库存量
- `turnover_rate`：周转率
- `turnover_days`：周转天数
- `stock_sales_rate`：库销比
- `contribution_rate`：贡献率
- `gmroi`：GMROI
- `first_sale_date`：首次销售日期
- `new_product`：新品标志
- `key_product`：重点商品标志

#### 客户分析

- `diag_result_customer_contribution`：客户贡献结果表
- `diag_customer_age_bucket`（字典）

关键字段：

- `gender`：性别
- `age_bucket_code`：年龄段编码
- `age_bucket_name`：年龄段名称
- `age_start`：年龄段起始值
- `age_end`：年龄段结束值
- `current_sales`：本期销售额
- `current_customer_count`：本期客数
- `current_customer_price`：本期客单价
- `current_unit_price`：本期件单价
- `current_sale_quantity`：本期销售量

---

## 5. 接口输入字段清单

### 5.1 诊断会话创建

#### `DiagnosisSessionCreateRequest`

- `classLevel`：品类层级
- `classNo`：品类编码
- `className`：品类名称
- `deptId`：组织/门店 ID
- `retailTypeId`：业态 ID
- `businessCircleId`：商圈 ID
- `deptGroupId`：门店组 ID
- `storeNo`：门店编码
- `periodStart`：本期开始日期
- `periodEnd`：本期结束日期
- `compareStart`：对比期开始日期
- `compareEnd`：对比期结束日期
- `extraFilterJson`：扩展过滤 JSON
- `triggerIfMissing`：缺少快照时是否自动触发预计算
- `waitSeconds`：等待秒数

### 5.2 品类树查询

#### `CategoryTreeQueryRequest`

- `deptId`：组织/门店 ID
- `retailTypeId`：业态 ID
- `businessCircleId`：商圈 ID
- `deptGroupId`：门店组 ID
- `storeNo`：门店编码
- `classLevel`：品类层级
- `classNo`：品类编码
- `classRole`：品类角色
- `skuAbnormal`：异常商品标志

### 5.3 全类检测

#### `AllClassCheckRequest`

- `deptId`：组织/门店 ID
- `retailTypeId`：业态 ID
- `businessCircleId`：商圈 ID
- `deptGroupId`：门店组 ID
- `storeNo`：门店编码
- `classLevel`：品类层级
- `classNo`：品类编码
- `classRole`：品类角色
- `currentStartDate`：本期开始日期
- `currentEndDate`：本期结束日期
- `compareStartDate`：对比期开始日期
- `compareEndDate`：对比期结束日期

### 5.4 预计算任务

#### `PrecomputeJobCreateRequest`

- `module`：模块编码
- `readRangeType`：读取范围类型
- `readStart`：读取开始日期
- `readEnd`：读取结束日期
- `compareStart`：对比期开始日期
- `compareEnd`：对比期结束日期
- `windowTypes`：窗口类型列表
- `forceRebuild`：是否强制重算
- `maxRetry`：最大重试次数
- `priority`：优先级
- `requestJson`：请求参数 JSON
- `submitterId`：提交人 ID

### 5.5 其他常见筛选项

当前系统的常见业务筛选维度包括：

- `status`：状态筛选
- `promotion`：促销筛选
- `brandTypeList`：品牌类型列表
- `brandList`：品牌列表
- `specList`：规格列表
- `tagType`：标签类型
- `tagList`：标签列表
- `abcType`：ABC 类型
- `currentAbc`：本期 ABC 等级
- `compareAbc`：对比期 ABC 等级
- `currentGrossRole`：本期毛利角色
- `compareGrossRole`：对比期毛利角色
- `currentGmroiRole`：本期 GMROI 角色
- `compareGmroiRole`：对比期 GMROI 角色
- `priceBandList`：价格带列表

---

## 6. 关键口径

以下口径已经在当前项目中明确或基本冻结：

- `sales`：销售额
- `sale_quantity`：销售量
- `gross`：毛利额
- `gross_rate`：毛利率，公式为 `gross / sales * 100`
- `customer_count`：客数，公式为 `COUNT(DISTINCT sale_no)`
- `customer_price`：客单价，公式为 `sales / customer_count`
- `avg_inventory`：平均库存，公式为 `周期内每日库存成本求和 / 天数`
- `inventory_sales`：库销比，公式为 `avg_inventory / sales`
- `turnover_days`：周转天数，公式为 `avg_inventory / sales * 周期天数`
- `turnover_rate`：动销率，公式为 `动销 SKU / 总 SKU`
- `gmroi`：GMROI，公式为 `gross / avg_inventory`
- `classSku`：品类下有效状态商品去重数
- `动销SKU`：周期内有销售记录的 SKU

---

## 7. 当前还需要确认的点

### 7.1 品牌类型来源

当前 `brandType` 的稳定来源还没完全确认。

已知可能的线索：

- `product_tag_info`：商品标签表
- 独立品牌维表
- 业务侧品牌类型对照表

### 7.2 `skuAbnormal` 口径

品类树接口已经预留了 `skuAbnormal`，但最终异常商品分类规则还需要确认。

### 7.3 库存是否支持子类维度过滤

如果 `fact_stock_day` 无法按子类过滤，部分库存相关指标只能返回 `0` 或退化计算。

### 7.4 字典口径

至少需要确认：

- `class_role_type`：品类角色字典类型
- 客户年龄段字典 `diag_customer_age_bucket`
- 价格带策略配置
- ABC 参数配置

---

## 8. 建议的数据接入顺序

1. 先接门店 / 品类 / 商品主数据
2. 再接销售事实和库存事实
3. 再接会员 / 标签 / 供应商 / 损耗
4. 最后补字典和配置
5. 再落预计算表和结果表

这样可以先跑通最小闭环，再逐步补齐分析模块。
