# 待解决问题清单

## 品牌分析

### 1. brandType 来源未确定

- 背景：
  - 品牌分析当前已确认业务口径为：
    - `2 = 自由品牌`
    - `1 = 其他品牌`
    - `"/" = 无品牌`
  - 但在 [Datainterface_YY.sql](/e:/new/backend/category-analysis/Datainterface_YY.sql:1) 中，当前只明确发现：
    - [base_dept_products](/e:/new/backend/category-analysis/Datainterface_YY.sql:394) 存在 `brand_no`、`product_brand`
    - [base_product](/e:/new/backend/category-analysis/Datainterface_YY.sql:928) 存在 `brand_no`、`product_brand`
  - 尚未发现可直接映射 `brandType` 的字段，例如：
    - `brand_type`
    - `own_brand`
    - `own_brand_no`
    - `brandType`

- 当前影响：
  - 品牌预计算里 `brandType` 只能做临时兜底：
    - `brandNo` 为空或 `"/"` -> `brandType = "/"`
    - 其他默认 `brandType = "1"`
  - 因此当前版本无法准确识别 `2 = 自由品牌`

- 待确认方向：
  - 是否存在独立品牌维表，可直接映射 `brand_no -> brandType`
  - [product_tag_info](/e:/new/backend/category-analysis/Datainterface_YY.sql:4174) 是否承载“自由品牌/自有品牌”标签
  - 如果 `product_tag_info` 可用，需要明确：
    - `tag_type_no`
    - `tag_type_name`
    - `tag_no`
    - `tag_name`
    中哪组编码代表“自由品牌”
  - 如果数据库中没有现成字段或标签，需要业务侧提供品牌类型对照表

- 后续处理建议：
  - 优先查现有系统是否已有品牌类型映射逻辑
  - 若没有，则补一张品牌类型映射表，预计算时统一产出：
    - `brandNo`
    - `productBrand`
    - `brandType`
    - `brandTypeName`
